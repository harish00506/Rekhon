# [5.2] Safe-to-Spend card

**Status:** Done (2026-08-16, v0.5.2) — merged to `dev` as `4d61509`  
**Epic:** Epic 5 — Dashboard, Export & Widget  
**Priority:** High  
**Depends on:** 5.1, 4.3  
**Assignee:** Harish G  
**Labels:** `dashboard`, `engine`

---

## Description

The Safe-to-Spend figure - income minus commitments, bills, and goal contributions across the period - computed by the engine and shown with its breakdown.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §5.2, §14; AI-STS  
**Design spec section:** §4 AI Architecture, §6 Module Structure  
**Epic summary:** The home dashboard, Safe-to-Spend, privacy blur, local JSON export/import, and the home-screen widget - the user's daily surfaces.

**Branch:** `feature/5-2-safe-to-spend-card`

## Tracker

Progress and phase checklist: [5.2-safe-to-spend-card-tracker.md](5.2-safe-to-spend-card-tracker.md)

---

## Acceptance Criteria

- [x] Safe-to-Spend is computed deterministically from nature + upcoming commitments (P-03), in `Money` paise — `SafeToSpendEngine` (AI-STS), `spentToDate` = issue 4.3's `NatureBreakdown.trueSpend`.
- [x] The card shows the breakdown/rule that produced it (P-02); never a black-box number — `SafeToSpend.lines` is part of the result and the constructor asserts they sum to the headline.
- [x] Golden-file test; airplane-mode safe — 12-record fixture fixing the breakdown as well as the figure; verified on a device in airplane mode.
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched.
- [x] All Skill Rules and label-specific rules below are satisfied.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11) — step 10's merge/PR is the one step left, held for the user.

Deviation from §5.2's literal formula (a 5% buffer, and a stand-in for goal contributions until
issue 7.1) is recorded in **[ADR-0021](../adr/0021-safe-to-spend-buffer-and-goal-stand-in.md)**.

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `compose-ui` | Compose screens: immutable UiState/StateFlow (ARC-004), M3 tokens, strings.xml/ICU, a11y, Paparazzi. | `~/.claude/skills/compose-ui/SKILL.md` |
| `compose-performance` | Keep Compose skippable: stable/immutable UiState (ARC-004), deferred reads, lazy keys, Baseline Profiles. | `~/.claude/skills/compose-performance/SKILL.md` |
| `source-driven-development` | Ground framework/library use (Compose, Room, Hilt, ML Kit) in official docs before coding. | `~/.claude/skills/source-driven-development/SKILL.md` |
| `new-engine` | Scaffold a :domain:engines:* module (one interface, provenance, Hilt, ENGINE.md, tests). | `.claude/skills/new-engine/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **AI-ARC-003/006** - every engine result carries provenance (engineId/version/inputWindow/computedAt/confidence/evidence).
- **§21.6 theming/i18n** - strings in `strings.xml` (ICU plurals); colours/dimensions from theme tokens.

---

## Workflow Rules — Before Starting

- [ ] Dependencies (5.1, 4.3) are complete or explicitly waived by the user
- [ ] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [ ] Read the SRS sections cited above (§5.2, §14; AI-STS) and quote the requirement IDs back
- [ ] Read the design spec section: **§4 AI Architecture, §6 Module Structure**
- [ ] Load the skill files listed above for this issue's labels
- [ ] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Deterministic number + shown breakdown (P-03/P-02).

### Label-specific

1. Exactly one public interface + result types; the implementation is `internal`, Hilt-injected (ARC-003).
2. Every result carries provenance: engineId/version/inputWindow/computedAt/confidence/evidence (AI-ARC-003).

### Epic-specific

1. Every figure on a surface is engine-computed and shown with its breakdown (P-03/P-02).

---

## Workflow Rules — Definition of Done

- [ ] Acceptance criteria checked off above
- [ ] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [ ] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [ ] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [ ] No secrets/PII/amounts in logs or committed files
- [ ] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [ ] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [ ] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

Done — 2026-08-16. Full timestamped log in the tracker; headlines:

```text
./gradlew ktlintCheck detekt lintDebug     OK — no new warnings
./gradlew unitTests koverVerify            OK — new engine 93.5% instruction / 92.6% line
./gradlew verifyPaparazziDebug             OK — 4 baselines re-recorded (light/dark/200%)
# CfoTest emulator, AIRPLANE MODE:
#   no income basis      -> "Not worked out yet" (never Rs 0, P-03)
#   + Rs 60,000 income   -> +Rs 56,421.00  (60,000 - 3,000 buffer - 579 spent), Rule RULE-STS v1.0
#   + Rs 1,000 expense   -> +Rs 55,421.00, "Already spent" 579 -> 1,579 in the same emission
```

Both new gates were **watched go red** before being trusted: a mis-labelled golden record and an
edited `buffer_pct` each fail the build.

Full timestamped log: [5.2-safe-to-spend-card-tracker.md](5.2-safe-to-spend-card-tracker.md#verification-log).

---

## Files Changed

Full table with what each path does now: the
[session file](../sessions/2026-08-16-safe-to-spend-card.md#3--code-changed-this-session).

| Path | Action |
|------|--------|
| `ai/rules/rules-kb.json` | Added `RULE-STS` v1.0; `_meta.version` → 1.12.0 |
| `ai/orchestrator/engine-registry.yaml` | Added the missing AI-STS row (L5) |
| `settings.gradle.kts` · `domain/engines/safetospend/**` | New pure-Kotlin engine module: interface, result types, rules mirror, impl, `ENGINE.md`, 4 test classes + golden fixture |
| `data/repository/SafeToSpendRepository.kt` (+ `RepositoryFactory`, `build.gradle.kts`, test) | Resolves the five terms, dedupes bills against scheduled rows, emits `null` for no income basis |
| `app/di/EngineModule.kt` · `app/di/RepositoryModule.kt` | Bind the engine and the repository |
| `feature/dashboard/**` | `safeToSpend: SafeToSpend?`, `observeSafeToSpend()`, `SafeToSpendSection`, 7 new strings, fake + tests, 4 re-recorded baselines |
| `domain/engines/budget/BudgetRules.kt` | `RULEBOOK_VERSION` restated to 1.12.0 (file revision, no threshold changed) |
| `docs/adr/0021-*.md` · `DECISIONS.md` · `FLOW.md` · `docs/sessions/2026-08-16-*.md` | The deviation, its index row, the new call path, the session record |
| `VERSION` · `app/build.gradle.kts` · `CHANGELOG.md` | 0.5.2, `versionCode` 19 |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/5.2-safe-to-spend-card/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
