# Issue 5.1 — Home dashboard v1 Tracker

**Parent issue:** [5.1-home-dashboard-v1.md](5.1-home-dashboard-v1.md)  
**Strategy:** The home dashboard: net worth, this-month cash flow, budgets, and recent activity - the app's landing surface, offline and accessible.  
**Branch (integration):** `dev`  
**Branch (implementation):** `feature/5-1-home-dashboard-v1` → merged to `dev`

---

## Status Summary

| Field | Value |
|-------|-------|
| **Status** | Done — code complete, static analysis and coverage green, device-verified |
| **Progress** | 10 / 11 phases |
| **Last verified** | 2026-08-15 |
| **VERSION at start** | 0.4.5 → 0.5.0 at ship |
| **Depends on** | 2.6, 4.4 |
| **Branch** | `feature/5-1-home-dashboard-v1` |

---

## Phase Checklist

| # | Phase | Status | Verification |
|---|-------|--------|--------------|
| 1 | Dependencies clear or waived | [x] | 2.6 and 4.4 shipped (4.4/4.5/4.6 live on this branch's ancestor, `feature/4-6-monthly-budget-review`, not yet on `dev` — see the session file) |
| 2 | Branch created | [x] | `feature/5-1-home-dashboard-v1`, branched from that commit rather than `dev` — same reason |
| 3 | Failing tests written (TDD) | [x] | `TransactionRepositoryTest` cases for both new reads written alongside the DAO query; `DashboardViewModelTest` cases for the four new collectors |
| 4 | Implementation to acceptance criteria | [x] | see the issue's Files Changed table |
| 5 | Static analysis clean | [x] | `./gradlew ktlintCheck detekt lintDebug` — clean after fixing five `MaxLineLength`/one multiline-expression violation the first run caught |
| 6 | Unit + coverage gate | [x] | `./gradlew unitTests koverVerify` — OK |
| 7 | Screenshot tests | [x] | `./gradlew :feature:dashboard:recordPaparazziDebug` then `verifyPaparazziDebug` — OK; `feature/dashboard`'s first Paparazzi tests, plugin newly applied |
| 8 | Run on emulator (real gate) | [x] | `/run` + `/verify` on `CfoTest` (AVD) — see log; found and fixed a real scrolling bug this way |
| 9 | E2E smoke + airplane-mode | [x] | Manual device pass with airplane mode confirmed on throughout (below); no `connectedDebugAndroidTest` case added for this issue specifically — the manual pass covered it and no new DB migration needed the heavier harness |
| 10 | Docs + VERSION + CHANGELOG | [x] | `FLOW.md`, `VERSION` 0.4.5 → 0.5.0, `CHANGELOG.md` done |
| 11 | Merge to `dev` + push | [ ] | not attempted; the user has not asked for a push |

---

## Verification Log

Append one row per verify step (lint, unit, coverage, screenshot, emulator run, E2E, git push).
Use `OK`, `SKIP - <reason>`, or `FAIL - <reason>`. `SKIP` must name why (never to go green).

| Date & time (UTC+5:30) | Phase | Command | Result |
|------------------------|-------|---------|--------|
| 2026-08-15 | 3-4 | `TransactionDao.observeRecent`/`observeMonthCashFlow`, repository impls + tests; dashboard state/ViewModel/screen + tests | OK — see the issue's Files Changed table |
| 2026-08-15 | 4 | First `observeMonthCashFlow` implementation combined two `observeDayTotals` Flows via `combine()` | FAIL — `kotlinx.coroutines.test`: "Detected use of different schedulers" when two Room query flows met inside `combine` under `UnconfinedTestDispatcher` |
| 2026-08-15 | 4 | Rewritten as one `CASE WHEN` SQL statement (`TransactionDao.observeMonthCashFlow`), no `combine()` | OK — also simpler and one query instead of two |
| 2026-08-15 | 5 | `./gradlew ktlintCheck detekt lintDebug` | FAIL then OK — five `MaxLineLength` violations (detekt) and one "multiline expression should start on a new line" (ktlint) across the new test files; wrapped, reran, clean |
| 2026-08-15 | 6 | `./gradlew unitTests koverVerify` | OK |
| 2026-08-15 | 7 | `./gradlew :feature:dashboard:recordPaparazziDebug` then `verifyPaparazziDebug` | OK — four baselines: loaded light/dark/200%, empty light |
| 2026-08-15 | 8 | `/run`: `installDebug` + launch on `CfoTest` (AVD), over existing multi-session profile data | OK — no crash; every new section rendered real figures (cash flow, budget status with its `RULE-BUD-ALERT v1.0` citation, five recent-activity rows) |
| 2026-08-15 | 8 | **Found on-device, not by a test:** the dashboard's `Column` has never scrolled (issue 1.10's four-row screen never needed to); this issue's three added sections pushed every navigation button off-screen and unreachable | FAIL — a real usability bug |
| 2026-08-15 | 8 | Added `.verticalScroll(rememberScrollState())`, rebuilt, reinstalled, relaunched, scrolled to the bottom | OK — every button reachable; re-ran the full test/lint suite after the fix, still green; Paparazzi baselines unchanged (unconstrained-height snapshot renders identically with or without the modifier) |
| 2026-08-15 | 8 | Tapped "Your budgets" from the dashboard | OK — navigated to the real Budgets screen; its "1 category is over budget" and ₹330.00 spent agree exactly with the dashboard's own figures for the same category |
| 2026-08-15 | 9 | `adb shell settings get global airplane_mode_on` | `1` — confirmed on for the entire device session above (P-04) |
| - | 11 | `git push` / PR to `dev` | SKIP — not attempted; the user has not asked for a push |
