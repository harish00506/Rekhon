# Issue 1.1 — Gradle multi-module skeleton + version catalog + CI Tracker

**Parent issue:** [1.1-gradle-multi-module-skeleton-version-catalog-ci.md](1.1-gradle-multi-module-skeleton-version-catalog-ci.md)  
**Strategy:** Stand up the one-way multi-module Gradle project, a single version catalog, and the CI pipeline - the skeleton every later issue lands into.  
**Branch (integration):** `main`  
**Branch (implementation):** `feature/1-1-gradle-multi-module-skeleton-version-catalog-ci` → merged to `main`

---

## Status Summary

| Field | Value |
|-------|-------|
| **Status** | In progress — skeleton builds green on the feature branch; not yet merged |
| **Progress** | 6 / 9 phases (emulator run + merge/push pending) |
| **Last verified** | 2026-07-19 |
| **VERSION at start** | 0.1.0 |
| **Depends on** | None |
| **Branch** | `feature/1-1-1-gradle-skeleton` (per task 1.1.1) |

---

## Phase Checklist

| # | Phase | Status | Verification |
|---|-------|--------|--------------|
| 1 | Dependencies clear or waived | [x] | deps: None |
| 2 | Branch created | [x] | `git branch --show-current` → `feature/1-1-1-gradle-skeleton` |
| 3 | Failing tests written (TDD) | [~] | ARC-002 TestKit test (`Arc002GuardTest`) + `Arc002Test` written to AC3 and verified failing→green; module stubs are scaffolding, not red-first |
| 4 | Implementation to acceptance criteria | [x] | AC1 (graph lists), AC2 (build green, 0 ktlint/detekt), AC3 (ARC-002 fails build) met; AC4 CI wired, runs on first PR |
| 5 | Static analysis clean | [x] | `./gradlew ktlintCheck detekt lintDebug` → OK (via `build`) |
| 6 | Unit + coverage gate | [x] | `./gradlew testDebugUnitTest koverVerify` → OK (no threshold teeth yet; real gates land with engines) |
| 7 | Run on emulator (real gate) | [ ] | SKIP for now — skeleton has only a placeholder screen; `/run` on first real feature |
| 8 | Docs + VERSION + CHANGELOG | [~] | `CHANGELOG.md` [Unreleased] updated; `VERSION` stays 0.1.0 (patch bump at ship) |
| 9 | Merge to `main` + push | [ ] | pending — open PR `feature/1-1-1-gradle-skeleton → dev` |

---

## Verification Log

Append one row per verify step (lint, unit, coverage, screenshot, emulator run, E2E, git push).
Use `OK`, `SKIP - <reason>`, or `FAIL - <reason>`. `SKIP` must name why (never to go green).

| Date & time (UTC+5:30) | Phase | Command | Result |
|------------------------|-------|---------|--------|
| 2026-07-19 | 4 | `./gradlew projects` | OK — full §21.2 graph lists (AC1) |
| 2026-07-19 | 5 | `./gradlew ktlintFormat` then `./gradlew build` | OK — assemble + lint + ktlint + detekt green across all modules (AC2) |
| 2026-07-19 | 6 | `./gradlew testDebugUnitTest koverVerify` (via `build`) | OK — all module smoke tests pass; koverVerify green |
| 2026-07-19 | 3/4 | `./gradlew -p build-logic :convention:test` | OK — `Arc002Test` + `Arc002GuardTest` (TestKit) pass; ARC-002 guard fails a build that applies an Android plugin to a Kotlin module (AC3) |
| 2026-07-19 | 4 | `./gradlew assembleDebug` | OK — `app/build/outputs/apk/debug/app-debug.apk` produced (AC3 assemble) |
| 2026-07-19 | 7 | `/run` + `/verify` | SKIP — skeleton has only a placeholder screen; deferred to first real feature |
| 2026-07-19 | 9 | CI on PR | PENDING — runs when `feature/1-1-1-gradle-skeleton → dev` PR opens |
