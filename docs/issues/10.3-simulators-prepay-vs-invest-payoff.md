# [10.3] Simulators (prepay-vs-invest, payoff)

**Status:** Done — merged to `dev` (0.10.2)  
**Epic:** Epic 10 — Advisor & Chat  
**Priority:** Medium  
**Depends on:** 6.2, 7.5  
**Assignee:** Harish G  
**Labels:** `engine`, `simulator`

---

## Description

What-if simulators (loan prepay vs invest, debt-payoff strategies) that compute exact outcomes side-by-side - simulate, never execute (P-07).

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §36; simulators  
**Design spec section:** §4 AI Architecture  
**Epic summary:** The Purchase Advisor, buy list, what-if simulators, vehicle prediction, on-device chat + guardrail eval, market signals, and localisation.

**Branch:** `feature/10-3-simulators-prepay-vs-invest-payoff`

## Tracker

Progress and phase checklist: [10.3-simulators-prepay-vs-invest-payoff-tracker.md](10.3-simulators-prepay-vs-invest-payoff-tracker.md)

---

## Acceptance Criteria

- [x] Each simulator computes deterministic outcomes in `Money` paise / bps and compares scenarios with the math shown (P-02) — AI-SIM 1.0, `Money.percentOf(bps, over 12)` throughout; every card names the engine, its version and its rule.
- [x] No money is moved - simulation only (P-07) — `SimulatorRepository` has **no write methods at all**, and the screen says so in words.
- [x] Golden-file tests vs known scenarios — `src/test/resources/golden/simulator.txt`, produced by an independent Python oracle (`golden/simulator_oracle.py`).
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched — `Long` paise and integer bps only; the engines are pure and take `nowUtcMillis` as an input (P-08); no network on any path (P-01/P-04).
- [x] All Skill Rules and label-specific rules below are satisfied — one public interface per engine, `internal` implementations behind `SimulatorFactory`, provenance on every result.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`) — see the Verification section below.

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `new-engine` | Scaffold a :domain:engines:* module (one interface, provenance, Hilt, ENGINE.md, tests). | `.claude/skills/new-engine/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `source-driven-development` | Ground framework/library use (Compose, Room, Hilt, ML Kit) in official docs before coding. | `~/.claude/skills/source-driven-development/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **AI-ARC-003/006** - every engine result carries provenance (engineId/version/inputWindow/computedAt/confidence/evidence).
- **P-07 Advice, never orders** - the app recommends and simulates; the user decides. No auto-executed money movement.

---

## Workflow Rules — Before Starting

- [x] Dependencies (6.2, 7.5) are complete or explicitly waived by the user — both merged
- [x] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [x] Read the SRS sections cited above (§36; simulators) and quote the requirement IDs back — §36 stages 6–7, §40.2 CRD-005, §34.3 FLT-004
- [x] Read the design spec section: **§4 AI Architecture**
- [x] Load the skill files listed above for this issue's labels
- [x] List assumptions and challenge each one before coding — the "reuse `PurchaseRequest`" note left by 10.2 was challenged and rejected: a what-if is about a debt, not a purchase

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Simulate, don't execute (P-07); exact money math (MNY-001/002).

### Label-specific

1. Exactly one public interface + result types; the implementation is `internal`, Hilt-injected (ARC-003).
2. Every result carries provenance: engineId/version/inputWindow/computedAt/confidence/evidence (AI-ARC-003).

### Epic-specific

1. Advisors simulate and recommend - never execute money movement (P-07); the LLM only verbalises (P-03).

---

## Workflow Rules — Definition of Done

- [x] Acceptance criteria checked off above
- [x] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [x] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%) — `koverVerify` green
- [x] Works offline - verified with an airplane-mode case where a data path is touched (P-04) — the whole device run was in airplane mode (`settings get global airplane_mode_on` → `1`)
- [x] No secrets/PII/amounts in logs or committed files
- [x] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI) — lint steady at 0 errors / 86 warnings; every string in `strings.xml` with an ICU plural for months; dark mode checked on the device
- [x] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [x] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

```text
./gradlew unitTests koverVerify ktlintCheck detekt lintDebug :app:assembleRelease :app:assembleDebug
  → BUILD SUCCESSFUL · 4,631 tests, 0 failures · lint 0 errors / 86 warnings (unchanged)

/run + /verify on the CfoTest emulator, in airplane mode (airplane_mode_on = 1):
  onboarding → a real profile → a ₹20,00,000 loan at 9% over 240 months added in the accounts editor
  Dashboard → "What if?" →
    prepay ₹2,00,000 at 12% less 30% tax
      → "Prepaying saves ₹4,32,934.85 in interest and ends the loan 28 months sooner."
      → "Investing the same money would leave you ₹6,04,935.74 after tax."
      → "Investing is ahead, by ₹1,72,000.89."  ·  flips at about 10% a year
      → "From AI-SIM v1.0 · RULE-PREPAY-VS-INVEST"
    payoff, ₹0 spare  → dearest/smallest first: 168 months, ₹16,34,474.00 of interest
    payoff, ₹15,000   → 75 months, ₹6,67,027.23 — the spare money and the rollover bite
      → "From AI-SIM v1.0 · RULE-PAYOFF-ORDER"
  empty states seen on the demo profile: "No loans on file…" and "Nothing is owed…"
  dark mode checked on the same screen; "Nothing has been paid or moved" is always the last line.
```

Full timestamped log: [10.3-simulators-prepay-vs-invest-payoff-tracker.md](10.3-simulators-prepay-vs-invest-payoff-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `domain/engines/simulator/**` | **new** — AI-SIM 1.0: both engines, `SimulatorMath`, the rulebook mirror, `ENGINE.md`, 35 tests, the golden file and its Python oracle |
| `data/repository/src/main/kotlin/.../SimulatorRepository.kt` | **new** — the household's debts and the two simulations; no write methods (P-07) |
| `data/repository/src/test/kotlin/.../SimulatorRepositoryTest.kt` | **new** — 7 tests, incl. the card that is left out rather than guessed at |
| `feature/advisor/.../Simulators{UiState,ViewModel,Screen}.kt`, `SimulatorResults.kt` | **new** — the "What if?" screen and its readouts |
| `feature/advisor/src/test/kotlin/.../Simulators*Test.kt`, `FakeSimulatorRepository.kt` | **new** — 13 UI/ViewModel tests |
| `feature/advisor/src/main/res/values/strings.xml` | the screen's strings, with an ICU plural for months |
| `app/.../di/RepositoryModule.kt`, `navigation/CfoRoute.kt`, `navigation/CfoNavHost.kt` | AI-SIM provided; the route to the screen |
| `feature/dashboard/DashboardScreen.kt`, `DashboardDestinations.kt`, `strings.xml` | the "What if?" action |
| `ai/rules/rules-kb.json`, `ai/rules/rulebook.md`, fourteen `*Rules.kt` mirrors | 1.23.0 — both rows now name AI-SIM; no threshold changed |
| `docs/adr/0051-…`, `DECISIONS.md`, `FLOW.md` §2.15, `ai/orchestrator/engine-registry.yaml` | the records |
| `VERSION`, `app/build.gradle.kts`, `CHANGELOG.md`, `docs/memory.md`, `docs/sessions/2026-09-26-simulators.md` | 0.10.2 / versionCode 46 |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/10.3-simulators-prepay-vs-invest-payoff/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
