# [10.4] Vehicle & maintenance prediction (AI-VEH)

**Status:** Done — merged to `dev` (0.10.3)  
**Epic:** Epic 10 — Advisor & Chat  
**Priority:** Medium  
**Depends on:** 9.2  
**Assignee:** Harish G  
**Labels:** `engine`, `vehicle`

---

## Description

A vehicle running-cost and maintenance-due predictor driven by the vehicle-maintenance KB and usage, folded into the forecast.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §38; AI-VEH, `ai/knowledge/vehicle-maintenance-kb.json`  
**Design spec section:** §4 AI Architecture  
**Epic summary:** The Purchase Advisor, buy list, what-if simulators, vehicle prediction, on-device chat + guardrail eval, market signals, and localisation.

**Branch:** `feature/10-4-vehicle-maintenance-prediction-ai-veh`

## Tracker

Progress and phase checklist: [10.4-vehicle-maintenance-prediction-ai-veh-tracker.md](10.4-vehicle-maintenance-prediction-ai-veh-tracker.md)

---

## Acceptance Criteria

- [x] Service-due and running-cost predictions come from the versioned vehicle KB (data) + usage; deterministic with evidence — AI-VEH 1.0 over `vehicle-maintenance-kb.json` **v1.1**, mirrored and held to the file by `VehicleKbDriftTest`; every result carries the engine, its version and the rows it cited.
- [x] Predicted costs flow into the forecast (9.2) and are shown with the rule that fired (P-02) — `ItemSource.VEHICLE_PREDICTION`, asserted by three repository tests and observed on the device ("Oct 5, 2026 · Swift −₹5,500.00 (predicted from your vehicle)").
- [x] Golden-file test — `golden/vehicle.txt` over five households, produced by an **independent** Python oracle (`golden/vehicle_oracle.py`).
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched — `Long` paise and integer bps only; the engine is pure and takes `todayIsoDate`/`nowUtcMillis` as inputs (P-08); dates resolved in the profile zone through an injected clock (TIM-001/002); no network on any path (P-01/P-04).
- [x] All Skill Rules and label-specific rules below are satisfied — one public interface, `internal` implementation behind `VehicleEngineFactory`, provenance on every result.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`) — see the Verification section below.

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `new-engine` | Scaffold a :domain:engines:* module (one interface, provenance, Hilt, ENGINE.md, tests). | `.claude/skills/new-engine/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `source-driven-development` | Ground framework/library use (Compose, Room, Hilt, ML Kit) in official docs before coding. | `~/.claude/skills/source-driven-development/SKILL.md` |
| `add-rulebook-rule` | Add/change any financial threshold as a versioned data row in ai/rules/ (never hardcoded). | `.claude/skills/add-rulebook-rule/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **AI-ARC-003/006** - every engine result carries provenance (engineId/version/inputWindow/computedAt/confidence/evidence).

---

## Workflow Rules — Before Starting

- [x] Dependencies (9.2) are complete or explicitly waived by the user — 9.2 merged; its forecast is where these predictions land
- [x] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [x] Read the SRS sections cited above and quote the requirement IDs back — **note:** this file says §38, which is the tax engine. The registry, the knowledge base and the engine id all say **§12**, and the work follows §12 (ADR-0052 records the discrepancy).
- [x] Read the design spec section: **§4 AI Architecture**
- [x] Load the skill files listed above for this issue's labels
- [x] List assumptions and challenge each one before coding — the one that did not survive: that a mean of the odometer deltas would do. It would let one typo rewrite the plan, which is why the rate is a median of the pairwise slopes.

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. KB-driven (data, §29); explainable (P-02).

### Label-specific

1. Exactly one public interface + result types; the implementation is `internal`, Hilt-injected (ARC-003).
2. Every result carries provenance: engineId/version/inputWindow/computedAt/confidence/evidence (AI-ARC-003).

### Epic-specific

1. Advisors simulate and recommend - never execute money movement (P-07); the LLM only verbalises (P-03).

---

## Workflow Rules — Definition of Done

- [x] Acceptance criteria checked off above
- [x] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [x] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%) — `koverVerify` green
- [x] Works offline - verified with an airplane-mode case where a data path is touched (P-04) — the whole device run was in airplane mode (`airplane_mode_on` → 1)
- [x] No secrets/PII/amounts in logs or committed files
- [x] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI) — lint steady at 0 errors / 86 warnings; every string in `strings.xml` with ICU plurals for days, readings and services; dark mode checked on the device
- [x] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue — including the real 26 → 27 migration of an existing install
- [x] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

```text
./gradlew unitTests koverVerify ktlintCheck detekt lintDebug :app:assembleRelease :app:assembleDebug
  → BUILD SUCCESSFUL · 4,728 tests, 0 failures · lint 0 errors / 86 warnings (unchanged)

./gradlew :core:database:connectedDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=…migration.MigrationRoundTripTest
  → 27 tests on CfoTest(AVD), including 26 → 27: the four tables keep their rows, and the two
    unique indices refuse a duplicate reading and a duplicate renewal.

/run + /verify on the CfoTest emulator, in airplane mode (airplane_mode_on = 1):
  installed over the existing build → schema 26 → 27 migrated with the profile intact
  Dashboard → "Your vehicles" → add "Swift" (Hatchback)
    → "Next service around 2027-09-26." · "Expect ₹4,000.00 to ₹7,000.00." ·
      "Not enough readings yet to tell how far it goes each month."
  record a service: 2025-10-05, 40,000 km, ₹5,000
    → "Next service around 2026-10-05 — it is due on the calendar before the odometer."
    → "Service due in 9 days."
  record two readings: 2026-07-28 @ 44,000 and 2026-09-26 @ 48,000
    → "About 2000 km a month, from your readings." · "2 readings, 1 service recorded."
  Dashboard → the next 90 days
    → "Going out ₹5,500.00" · "Lowest point: ₹2,44,500.00 on Oct 5, 2026"
    → "Oct 5, 2026 · Swift −₹5,500.00 (predicted from your vehicle)"
  dark mode checked on the same screen; all five class chips wrap onto two rows and stay tappable.
```

Full timestamped log: [10.4-vehicle-maintenance-prediction-ai-veh-tracker.md](10.4-vehicle-maintenance-prediction-ai-veh-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `ai/knowledge/vehicle-maintenance-kb.json` | **1.0 → 1.1** — typed `prediction` and `alerts` blocks in place of prose |
| `domain/engines/vehicle/**` | **new** — AI-VEH 1.0: the engine, `VehicleMath`, the KB mirror, `ENGINE.md`, 38 tests, the golden file and its Python oracle |
| `core/database/entity/Entities.kt`, `dao/Daos.kt`, `CfoDatabase.kt`, `migration/Migrations.kt` | schema **27**: four tables, `VehicleDao`, `MIGRATION_26_27`; the demo wipe and residue count repaired for all eight of Epic 10's tables |
| `core/database/src/androidTest/**/MigrationRoundTripTest.kt` | the 26 → 27 case, including both unique-index refusals |
| `data/repository/VehicleRepository.kt` | **new** — the household's vehicles and their predictions |
| `data/repository/src/test/**/VehicleRepositoryTest.kt` | **new** — 12 tests |
| `data/repository/ForecastRepository.kt` (+ its test) | predicted costs join the ninety-day horizon |
| `data/repository/{Archive,ArchiveRepository,RepositoryFactory,DemoModeRepository}.kt`, `sharedTest/DrillFixture.kt` | the four tables through backup, restore, wipe and drill |
| `domain/engines/forecast/ForecastEngine.kt` | `ItemSource.VEHICLE_PREDICTION` |
| `feature/vehicle/**` | **new** — the screen, its strings and 19 tests |
| `feature/dashboard/**` | the "Your vehicles" action and the forecast's wording for the new source |
| `app/.../di/RepositoryModule.kt`, `navigation/CfoRoute.kt`, `navigation/CfoNavHost.kt`, `app/build.gradle.kts` | AI-VEH provided; the route |
| `docs/adr/0052-…`, `DECISIONS.md`, `FLOW.md` §2.16, `ai/orchestrator/engine-registry.yaml` | the records |
| `VERSION`, `CHANGELOG.md`, `docs/memory.md`, `docs/sessions/2026-09-26-vehicle-maintenance.md` | 0.10.3 / versionCode 47 |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/10.4-vehicle-maintenance-prediction-ai-veh/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
