# Issue 4.4 — Budgets CRUD + suggestions Tracker

**Parent issue:** [4.4-budgets-crud-suggestions.md](4.4-budgets-crud-suggestions.md)  
**Strategy:** Per-category budgets with deterministic, evidence-backed suggestions derived from history - the user always decides the final number (P-07).  
**Branch (integration):** `dev`  
**Branch (implementation):** `feature/4-4-budgets-crud-suggestions` → merged to `dev`

---

## Status Summary

| Field | Value |
|-------|-------|
| **Status** | Code complete; verified on a device. Not merged, not pushed |
| **Progress** | 8 / 9 phases |
| **Last verified** | 2026-08-12 |
| **VERSION at start** | see repo-root `VERSION` |
| **Depends on** | 4.1 |
| **Branch** | `feature/4-4-budgets-crud-suggestions` |

---

## Phase Checklist

| # | Phase | Status | Verification |
|---|-------|--------|--------------|
| 1 | Dependencies clear or waived | [x] | 4.1 present; 4.1–4.3 are unmerged commits on the branch below, so this was cut from HEAD rather than `dev` |
| 2 | Branch created | [x] | `feature/4-4-budgets-crud-suggestions` |
| 3 | Failing tests written (TDD) | [x] | golden + both drift tests + the split regression each watched red first |
| 4 | Implementation to acceptance criteria | [x] | FR-BUD-001/002/003; 004/005 are 4.5/4.6 and out of scope |
| 5 | Static analysis clean | [x] | `ktlintCheck detekt lintDebug` — BUILD SUCCESSFUL |
| 6 | Unit + coverage gate | [x] | `unitTests koverVerify` — BUILD SUCCESSFUL; engine at 99.5% |
| 7 | Run on emulator (real gate) | [x] | CRUD + FR-BUD-003 figures + offline verified on `CfoTest` (API 34). Caught one real defect (`0e6aa41`). Three rows skipped with reasons; one unrelated SEC-002 crash reported |
| 8 | Docs + VERSION + CHANGELOG | [x] | ENGINE.md, ADR-0017, ADR-0018; VERSION 0.4.2 → 0.4.3 |
| 9 | Merge to `dev` + push | [ ] | not started — awaiting phase 7 and an explicit instruction to push |

---

## Verification Log

Append one row per verify step (lint, unit, coverage, screenshot, emulator run, E2E, git push).
Use `OK`, `SKIP - <reason>`, or `FAIL - <reason>`. `SKIP` must name why (never to go green).

| Date & time (UTC+5:30) | Phase | Command | Result |
|------------------------|-------|---------|--------|
| 2026-08-11 | 3 | Break `RULE-BUD-SUGGEST.lookback_months` in `ai/rules/rules-kb.json` | OK — `RulebookDriftTest` red. Proves the Gradle `inputs.file` wiring too: only an `ai/` file changed, and the test task still re-ran |
| 2026-08-11 | 3 | Break a seasonality multiplier in `calendar-seasonality.json` | OK — `SeasonalityKbDriftTest` red |
| 2026-08-11 | 3 | Corrupt one golden record's expected amount | OK — `BudgetGoldenTest` red on that record alone |
| 2026-08-11 | 3 | Drop a cited rule from a golden record | OK — the meta-test red, so the gate cannot pass vacuously |
| 2026-08-11 | 3 | Reintroduce the pre-4.4 split defect (`s.category_id` → `t.category_id`, both projection and join) then `:data:repository:testDebugUnitTest --rerun-tasks` | OK — `a split payment is classified by its lines, not by its empty parent` red: `expected Money(minor=300000) but was Money(minor=0)`. Defect reverted, suite green |
| 2026-08-11 | 6 | `./gradlew :domain:engines:budget:koverVerify` | OK — 190/191 lines (99.5%), above the 85% floor |
| 2026-08-11 | 5 | `./gradlew ktlintCheck detekt lintDebug` | OK — BUILD SUCCESSFUL, no new warnings |
| 2026-08-11 | 6 | `./gradlew unitTests koverVerify` | OK — BUILD SUCCESSFUL across all modules |
| 2026-08-11 | 8 | `python -c "json.load(...)" ai/**/*.json` | OK — every knowledge file still parses |
| 2026-08-11 | 8 | `./gradlew :app:assembleDebug` | OK — Hilt graph resolves with `PlatformModule` and the budget bindings |
| 2026-08-12 14:15 | 7 | `emulator -avd CfoTest`, `adb install -r -t app-debug.apk`, launch | OK — app starts, no crash; **airplane mode already on**, so everything below is the P-04 offline test |
| 2026-08-12 14:16 | 7 | Dashboard → **Your budgets** | OK — new button renders under the 50/30/20 bar; budgets screen opens offline, Groceries ₹250 listed under "Spending with no budget" |
| 2026-08-12 14:16 | 7 | Read the spend figure on screen | **FAIL — ₹250 of spending rendered green**, the colour this app uses for money *in*. `CfoAmountText` colours by sign and spend is a positive magnitude. Fixed in `0e6aa41` with `BudgetAmountText`; reinstalled and confirmed neutral. Not caught by any unit, Compose or lint gate — none of them assert colour |
| 2026-08-12 14:20 | 7 | Set a budget: ₹400 on Groceries, rollover on, Save | OK — sheet renders, **Save correctly disabled on an empty field**, row moves to "This month's plan" |
| 2026-08-12 14:20 | 7 | Check FR-BUD-003's figures against the engine (12 Aug, day 12 of 31) | OK — "₹150.00 left of ₹400.00"; safe pace ₹154.80 = `400 × (12×10000/31 bps)`; projection ₹645.82 = `250 × (31×10000/12 bps)`; "Ahead of pace" and "over budget" both flagged. Every figure reproduces the engine's integer-bps arithmetic exactly |
| 2026-08-12 14:22 | 7 | Force-stop, relaunch, reopen budgets | OK — the budget persisted, still offline |
| 2026-08-12 14:22 | 7 | **Remove** the budget | OK — row returns to "Spending with no budget" **and the ₹250 of spending survives**, which is what the no-confirmation decision rests on |
| 2026-08-12 14:25 | 7 | Back from budgets → dashboard | OK — dashboard restored; 4.3's "Needs ₹349 · Wants ₹150 · Kept ₹0.00" unchanged, as expected with no split rows in the demo data |
| 2026-08-12 14:21 | 7 | Intermittent crash on relaunch under load | **FAIL — not attributable to 4.4, reported not hidden.** `IllegalStateException: The encrypted database was opened while the app was locked (SEC-002)` at `CoreModule.provideDatabase`, reached from `AppLockGate` → `MainViewModel` — issue 2.2's code, untouched by this diff. Fired once while the emulator was in a System-UI ANR; **not reproducible in 4 subsequent force-stop/relaunch cycles** (0 FATAL). Needs its own issue |
| 2026-08-12 | 7 | Accept a suggestion | SKIP — the demo profile has no closed-month history, so `min_months_required` (2) is not met and no suggestion is offered. That is the correct behaviour and was observed; the accept path itself is covered by `BudgetRepositoryTest` and `BudgetsViewModelTest` |
| 2026-08-12 | 7 | Rollover carried into the next month | SKIP — needs a month boundary the device cannot cross without falsifying its clock. Covered by `BudgetRepositoryTest` (carry, no-deficit, opt-in) |
| 2026-08-12 | 7 | Split one transaction across two categories, both budgets move | SKIP — not exercised on device. Covered by 25 Room tests and by the regression proof above, where the defect was reintroduced and watched to fail |
