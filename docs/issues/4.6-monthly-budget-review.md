# [4.6] Monthly budget review

**Status:** Done — code complete, static analysis and coverage green, device-verified (tracker
phases 1-8; only the merge, phase 9, is outstanding and not requested)  
**Epic:** Epic 4 — Categorisation & Budgets  
**Priority:** Medium  
**Depends on:** 4.4  
**Assignee:** Harish G  
**Labels:** `budgets`

---

## Description

A monthly review that summarises budget vs actual per category and proposes next-month adjustments with evidence.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §5.5; FR-BUD-*  
**Design spec section:** §4 AI Architecture  
**Epic summary:** The category KB, Stage-1 auto-categorisation, Need/Want/Invest nature classification, and budgets with threshold alerts.

**Branch:** `feature/4-6-monthly-budget-review`

## Tracker

Progress and phase checklist: [4.6-monthly-budget-review-tracker.md](4.6-monthly-budget-review-tracker.md)

---

## Acceptance Criteria

- [x] A month-end summary (budget vs actual, `Money` paise) is computed deterministically on the injected `Clock`. — `BudgetEngine.review`/`BudgetMonthReview` (commit `9092496`); `BudgetRepository.observeReview` resolves the reviewed month via the injected `Clock`, never a wall-clock read.
- [x] Proposed adjustments show inputs + rule (P-02); the user accepts/edits (P-07). — `BudgetReviewCard.kt` shows the variance, both cited rules (`RULE-BUD-REVIEW`, `RULE-BUD-SUGGEST`) and an accept-only button; nothing writes until `BudgetsEvent.AcceptReviewProposal` arrives from a tap.
- [x] Golden-file test on a fixed month. — `BudgetGoldenTest`'s 8 `kind=review` records (commit `9092496`).
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched. — no `Double`/`Float` on money, no raw clock reads; `./gradlew ktlintCheck detekt lintDebug` clean; airplane mode verified end-to-end on device (P-04).
- [x] All Skill Rules and label-specific rules below are satisfied.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`). — see tracker phases 1-8; phase 9 (merge/push) intentionally not attempted, not requested.

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `add-rulebook-rule` | Add/change any financial threshold as a versioned data row in ai/rules/ (never hardcoded). | `.claude/skills/add-rulebook-rule/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **P-07 Advice, never orders** - the app recommends and simulates; the user decides. No auto-executed money movement.

---

## Workflow Rules — Before Starting

- [ ] Dependencies (4.4) are complete or explicitly waived by the user
- [ ] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [ ] Read the SRS sections cited above (§5.5; FR-BUD-*) and quote the requirement IDs back
- [ ] Read the design spec section: **§4 AI Architecture**
- [ ] Load the skill files listed above for this issue's labels
- [ ] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Deterministic, show-the-work, advice-only (P-02/P-07).

### Label-specific

1. Thresholds are versioned rule rows in `ai/rules/`, never hardcoded (§29).

### Epic-specific

1. Categorisation and budget thresholds are versioned data rows cited by ID - never hardcoded (§29).

---

## Workflow Rules — Definition of Done

- [x] Acceptance criteria checked off above
- [x] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [x] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [x] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [x] No secrets/PII/amounts in logs or committed files
- [x] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI) — dark mode/200% font verified via the shared design-system components already checked in issue 4.5's device run; see tracker log for the one SKIP and its reason
- [x] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [x] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

Green on the JVM and on device.

```text
./gradlew ktlintCheck detekt lintDebug             # OK (after 2 fixes — see tracker log)
./gradlew unitTests koverVerify                    # OK (after 1 fix — see tracker log)
./gradlew :core:database:connectedDebugAndroidTest # OK — 19/19, incl. the 14→15 migration
# /run + /verify on CfoTest (AVD)                  # OK — see tracker log for the full device walk-through
```

Full timestamped log: [4.6-monthly-budget-review-tracker.md](4.6-monthly-budget-review-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `ai/rules/rules-kb.json`, `ai/rules/rulebook.md` | New row `RULE-BUD-REVIEW` v1.0 (`min_variance_pct` 15, `proposal_basis` median, `review_once_per_month`); `_meta.version` → 1.11.0 (commit `9092496`) |
| `domain/engines/budget/.../BudgetEngine.kt` | Third-plus contract method `review(...)`; `BudgetReviewInput`, `ReviewedCategoryInput`, `VarianceDirection`, `ReviewedCategory`, `BudgetReview` (commit `9092496`) |
| `domain/engines/budget/.../BudgetMonthReview.kt` | **New** — the variance/materiality decision, calling back into `suggest` for pricing (commit `9092496`) |
| `domain/engines/budget/.../DefaultBudgetEngine.kt` | `review` implemented; `proposalFor` (commit `9092496`) |
| `domain/engines/budget/.../BudgetRules.kt` | `minVariancePct` mirror, `REVIEW` citation, `RULEBOOK_VERSION` → 1.11.0 (commit `9092496`) |
| `domain/engines/budget/.../{RulebookDriftTest,BudgetGoldenTest,BudgetEngineTest}.kt` | Review coverage: drift, 8 golden records, boundary/unpriceable/empty/dual-citation cases (commit `9092496`) |
| `domain/engines/budget/ENGINE.md` | `review` contract, materiality formula, `RULE-BUD-REVIEW`, known-limits and version-log entries |
| `core/database/.../entity/Entities.kt` | `BudgetReviewEntity` — `UNIQUE(profile_id, month_start_iso_date)`, no per-category detail (ADR-0020) |
| `core/database/.../dao/Daos.kt` | `BudgetReviewDao` (`insertIfNew` = IGNORE, `observeForMonth`); `DemoDao.deleteBudgetReviews` + `countRowsFor` term |
| `core/database/.../CfoDatabase.kt` | `VERSION` 14 → 15; entity + DAO registered |
| `core/database/.../migration/Migrations.kt` | `MIGRATION_14_15` — `CREATE TABLE` + unique index (non-destructive, DB-003) |
| `core/database/schemas/.../15.json` | **New** — exported schema, committed |
| `core/database/.../MigrationRoundTripTest.kt` | 14 → 15 case; duplicate-month claim asserted as `SQLiteConstraintException` |
| `core/database/.../MigrationSafetyTest.kt` | `budget_review` exemption argued (no tombstone, coarser key than `budget_alert`) |
| `data/repository/.../BudgetRepository.kt` | `observeReview`, `acceptReviewProposal`, `dismissReview`; `rawReview`/`reviewFor`/`reviewedMonth` helpers; `budgetReviewId` |
| `data/repository/.../BudgetRepositoryTest.kt` | Material/on-plan/empty findings, dismiss claim-once, accept-writes-current-month, demo isolation |
| `data/repository/.../DemoModeRepository.kt` | `exit()` now calls `deleteBudgetReviews`; also fixed a pre-existing gap — `deleteBudgetAlerts` (issue 4.5) was never called |
| `data/repository/.../DemoModeRepositoryTest.kt` | Regression cases for both residue gaps above |
| `feature/budgets/.../BudgetsUiState.kt` | `review` field; counted in `isEmpty`; `AcceptReviewProposal`/`DismissReview` events |
| `feature/budgets/.../BudgetsViewModel.kt` | `observeReview` collector (failure → `null`); `acceptReviewProposal`/`dismissReview` handlers |
| `feature/budgets/.../BudgetReviewCard.kt` | **New** — section + per-category card, totals, variance sentence, both rule citations, accept/dismiss |
| `feature/budgets/.../BudgetsScreen.kt` | `BudgetReviewSection` rendered below the alert banner, above the plan |
| `feature/budgets/src/main/res/values/strings.xml` | Review title, summary, over/under sentences, no-history line, dismiss label |
| `feature/budgets/.../{BudgetsViewModelTest,BudgetsFlowTest,FakeBudgetRepository}.kt` | Review reaches state, accept/dismiss routing, card rendering, `reviewRow` fixture |
| `docs/adr/0020-budget-review-keyed-by-month-not-category.md` | **New** — why the claim is keyed by month alone, not per category |
| `FLOW.md`, `DECISIONS.md` | Review call chain; pointer to ADR-0020 |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/4.6-monthly-budget-review/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
