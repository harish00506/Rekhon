# [2.1] 4-step onboarding flow

**Status:** Implemented and verified (not yet merged)  
**Epic:** Epic 2 — Onboarding, Security & Accounts  
**Priority:** High  
**Depends on:** 1.10, 1.9  
**Assignee:** Harish G  
**Labels:** `onboarding`, `ui`

---

## Description

The 4-step first-run onboarding (welcome → privacy/consent → profile & currency → quick setup) - offline, accessible, and consent-first.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §5.1, §23; FR-ONB-*  
**Design spec section:** §6 Module Structure, §8 Security & Privacy  
**Epic summary:** First-run onboarding, biometric app lock, and the accounts + net-worth core - the entry point and the entities balances are computed from.

**Branch:** `feature/2-1-4-step-onboarding-flow`

## Tracker

Progress and phase checklist: [2.1-4-step-onboarding-flow-tracker.md](2.1-4-step-onboarding-flow-tracker.md)

---

## Acceptance Criteria

- [x] The four-step flow renders and navigates; state survives process death; it is fully offline (P-04).
      *Proved on a device: `am kill` changed the PID and step 3, the typed name and the chosen zone
      all came back. The whole manual pass ran with airplane mode enabled.*
- [x] The consent screen writes per-feature consent rows (default off) before any optional feature is enabled (P-01).
      *The switch starts off; opting in writes `sms_parsing`, and declining writes **nothing** — no
      revocation record for a decision never made. Both confirmed in the on-disk proto.*
- [x] A Compose UI test drives the full flow; strings externalised; dark mode + 200% font verified.
      *`OnboardingFlowTest` (Robolectric, JVM) drives all four steps and covers dark mode and a 2.0
      font scale; `OnboardingFlowInstrumentedTest` repeats it on a device against real DataStore.
      Both were also observed by hand on the emulator.*
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched.
      *`money-time-audit` clean — see the tracker's last log row.*
- [x] All Skill Rules and label-specific rules below are satisfied.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11) — except step 10 (merge), which
      awaits the user's go-ahead.

**Deviation on record:** the four steps are the ones this issue specifies, not FR-ONB-001's
literal ordering — its steps 3 and 4 belong to issues 2.2 and 2.5, which 2.1 does not depend on.
See [ADR-0002](../adr/0002-onboarding-step-order.md), which also fixes where those steps insert.
**FR-ONB-001 is not closed by this issue alone.**

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `compose-ui` | Compose screens: immutable UiState/StateFlow (ARC-004), M3 tokens, strings.xml/ICU, a11y, Paparazzi. | `~/.claude/skills/compose-ui/SKILL.md` |
| `source-driven-development` | Ground framework/library use (Compose, Room, Hilt, ML Kit) in official docs before coding. | `~/.claude/skills/source-driven-development/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **§21.6 theming/i18n** - strings in `strings.xml` (ICU plurals); colours/dimensions from theme tokens.

---

## Workflow Rules — Before Starting

- [ ] Dependencies (1.10, 1.9) are complete or explicitly waived by the user
- [ ] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [ ] Read the SRS sections cited above (§5.1, §23; FR-ONB-*) and quote the requirement IDs back
- [ ] Read the design spec section: **§6 Module Structure, §8 Security & Privacy**
- [ ] Load the skill files listed above for this issue's labels
- [ ] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. No optional data path is enabled without the consent captured here (P-01).

### Label-specific

1. Consent is captured before any optional data path is enabled (P-01).
2. Every user-visible string lives in `strings.xml` with ICU plurals; colours/dimensions come from theme tokens (§21.6).
3. UiState is one immutable data class exposed as `StateFlow`; events flow up via a sealed interface (ARC-004).

### Epic-specific

1. Everything works offline; consent is captured before any optional feature is enabled (P-01/P-04).

---

## Workflow Rules — Definition of Done

- [ ] Acceptance criteria checked off above
- [ ] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [ ] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [ ] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [ ] No secrets/PII/amounts in logs or committed files
- [ ] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [ ] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [ ] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

All green on 2026-07-25 (UTC+5:30):

```text
./gradlew ktlintCheck detekt lint      OK — repo-wide, no new warnings, no baseline
./gradlew test koverVerify             OK — 202 tests, 0 failures, 0 skipped; :core:model still 100%
./gradlew verifyPaparazziDebug         OK — existing baselines unchanged (see the tracker's phase-7 note)
./gradlew build                        OK — every module, debug + release
./gradlew :feature:onboarding:connectedDebugAndroidTest   OK — 1 test on CfoTest (AVD), API 34
# emulator, airplane mode ON throughout: fresh install → onboarding; complete → dashboard;
# force-stop → dashboard; am kill mid-flow → same step and answers restored; dark mode + 200% font.
```

Three defects were found by these runs rather than by reading: a consent row whose label was not
tappable, a duplicated Indian time zone (`Asia/Calcutta` vs `Asia/Kolkata`), and a device-only race
where the store was read before the write landed. All three are fixed and covered by tests.

Full timestamped log, including every failure: [2.1-4-step-onboarding-flow-tracker.md](2.1-4-step-onboarding-flow-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `core/model/.../MoneyFormatter.kt` | Added `parse` — typed rupees → `Money`, exact via `BigInteger`, never `Double` (MNY-001) |
| `core/model/src/test/.../MoneyParseTest.kt` | Added — 10 tests incl. a `parse(format(m)) == m` round trip |
| `core/datastore/src/main/proto/cfo_settings.proto` | Added fields 6–10: completion timestamp, display name, three `int64` paise seeds |
| `core/datastore/.../Model.kt` | Added `QuickSetupSeeds`, `SettingsSnapshot.isOnboarded` and the new fields |
| `core/datastore/.../SettingsStore.kt` | Added `OnboardingProfile` + `completeOnboarding` — one atomic write, timestamp from the injected `Clock` |
| `core/datastore/.../CfoDataStoreFactory.kt` | Passes the `Clock` to the settings store |
| `core/datastore/build.gradle.kts` | `:core:model` → `api` (`Money` is now in the public surface) |
| `core/datastore/src/test/.../SettingsStoreTest.kt` | +5 tests: defaults, full round trip, skipped seeds, atomicity |
| `feature/onboarding/.../Onboarding{UiState,ViewModel,Screen,Steps,Options}.kt` | Added — the flow (ARC-004) |
| `feature/onboarding/src/main/res/values/strings.xml` | Added — all copy, incl. FR-ONB-003's required statements |
| `feature/onboarding/src/test/.../{FakeStores,OnboardingViewModelTest,OnboardingFlowTest,OnboardingOptionsTest}.kt` | Added — 31 JVM tests |
| `feature/onboarding/src/androidTest/.../OnboardingFlowInstrumentedTest.kt` | Added — the flow against real DataStore on a device |
| `feature/onboarding/.../ModulePlaceholder{,Test}.kt` | **Deleted** — the module has real code now |
| `feature/onboarding/build.gradle.kts` | Store deps, Robolectric, instrumentation runner |
| `app/.../MainViewModel.kt` + its test | Added — decides the start destination from the stored flag |
| `app/.../{MainActivity,navigation/CfoRoute,navigation/CfoNavHost}.kt` | Onboarding route; start destination resolved at launch; `popUpTo` inclusive on finish |
| `app/src/test/.../di/ProfileZoneProviderTest.kt` | Its fake store implements the new interface method |
| `app/build.gradle.kts` | Compose/Hilt bridges for `MainActivity`'s ViewModel |
| `gradle/libs.versions.toml` | Pinned Robolectric 4.16.1 |
| `config/detekt/detekt.yml` | `UnusedPrivateMember` ignores `@Preview` |
| `docs/adr/0002-onboarding-step-order.md` | Added — the FR-ONB-001 deviation |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/2.1-4-step-onboarding-flow/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
