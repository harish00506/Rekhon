# Issue 6.3 — Investments holdings/lots + XIRR Tracker

**Parent issue:** [6.3-investments-holdings-lots-xirr.md](6.3-investments-holdings-lots-xirr.md)  
**Strategy:** Investment holdings tracked by lots, with a deterministic XIRR/return engine over dated cash flows - the basis for allocation and net worth.  
**Branch (integration):** `dev`  
**Branch (implementation):** `feature/6-3-investments-holdings-lots-xirr` → merged to `dev`

---

## Status Summary

| Field | Value |
|-------|-------|
| **Status** | Code complete and device-verified; not committed |
| **Progress** | 8 / 9 phases |
| **Last verified** | 2026-08-25 |
| **VERSION at start** | 0.6.2 → **0.6.3** |
| **Depends on** | 2.5 |
| **Branch** | `feature/6-3-investments-holdings-lots-xirr` |

---

## Phase Checklist

| # | Phase | Status | Verification |
|---|-------|--------|--------------|
| 1 | Dependencies clear or waived | [x] | 2.5 (accounts CRUD) is Done — `AccountType.INVESTMENT/GOLD/CRYPTO` already exist |
| 2 | Branch created | [x] | `feature/6-3-investments-holdings-lots-xirr`, off `dev` |
| 3 | Failing tests written (TDD) | [x] | `AssetClassTest`/`InvestmentTest`/`QuantityTest` written red — `Unresolved reference 'AssetClass'` before the types existed |
| 4 | Implementation to acceptance criteria | [x] | every AC ticked in the issue file; the `Clock` deviation recorded there |
| 5 | Static analysis clean | [x] | all three green; `lintDebug` caught a real API-31 call on a minSdk-26 path |
| 6 | Unit + coverage gate | [x] | `unitTests koverVerify` green — `:core:model` at its 100% gate, the engine over 85% |
| 7 | Run on emulator (real gate) | [x] | AVD `CfoTest` (API 34) — holding created by hand, ₹8,250.00 / ₹750.00 gain / **10.0% a year** on screen; offline and blurred both verified |
| 8 | Docs + VERSION + CHANGELOG | [x] | ADR-0027, ADR-0028, `ENGINE.md`, FLOW §2.3, DECISIONS, CHANGELOG, VERSION 0.6.3 |
| 9 | Merge to `dev` + push | [ ] | **not done** — nothing committed or pushed; awaiting the user's go-ahead (§7) |

---

## Verification Log

Append one row per verify step (lint, unit, coverage, screenshot, emulator run, E2E, git push).
Use `OK`, `SKIP - <reason>`, or `FAIL - <reason>`. `SKIP` must name why (never to go green).

| Date & time (UTC+5:30) | Phase | Command | Result |
|------------------------|-------|---------|--------|
| 2026-08-24 | 3 | `./gradlew :core:model:test` (before the types existed) | OK — red as intended: `Unresolved reference 'AssetClass'` |
| 2026-08-24 | 3 | `./gradlew :core:model:test :core:model:koverVerify` | OK — green after slice 1; 100% gate met |
| 2026-08-24 | 4 | `./gradlew :core:database:test :data:repository:test` | OK — schema 18, migration + archive round trips |
| 2026-08-24 | 4 | `./gradlew :core:database:compileDebugAndroidTestKotlin` | OK — the 17→18 round-trip case compiles (it needs a device to run) |
| 2026-08-24 | 4 | `./gradlew :domain:engines:investment:test` | OK — 8 golden cases match an independent 60-digit implementation on the first run |
| 2026-08-24 | 4 | `./gradlew :domain:engines:investment:koverVerify` | OK — over the 85% engine gate |
| 2026-08-24 | 4 | `./gradlew :app:compileDebugKotlin` | OK — the Hilt graph compiles with the engine and repository bound |
| 2026-08-24 | 4 | `./gradlew :feature:accounts:test` | OK — after fixing a real bug the test caught: `editEditor` cleared `fieldError` in the same expression that set it |
| 2026-08-24 | 5 | `./gradlew ktlintCheck detekt` | OK — after extracting `AccountsActions`, `AccountRow.kt`, `AccountsInvestmentSummary.kt` and `accountsDestinations` to clear LongMethod/LongParameterList/TooManyFunctions |
| 2026-08-24 | 5 | `./gradlew lintDebug` | FAIL then OK — `BigInteger.longValueExact()` is API 31, minSdk is 26; it compiled and would have crashed on an older device. Replaced with `BigDecimal.setScale(0, HALF_EVEN).longValueExact()` |
| 2026-08-24 | 6 | `./gradlew unitTests koverVerify` | OK — whole project green, 6m 20s |
| 2026-08-24 | 7 | `/run` + `/verify` on an emulator | **SKIP — no emulator or device available in this environment.** §9 is explicit that a green `unitTests` does not close an issue, so this issue is **not** Done |
| 2026-08-24 | 7 | `./gradlew connectedDebugAndroidTest` | **SKIP — same reason.** The 17→18 migration round-trip and the airplane-mode case are unrun |
| 2026-08-25 | 7 | emulator `CfoTest` boot (`-no-window -gpu swiftshader_indirect`) | OK — booted in 41s, API 34, x86_64. An AVD **did** exist; the 2026-08-24 SKIP was wrong |
| 2026-08-25 | 7 | `./gradlew :core:database:connectedDebugAndroidTest` | OK — 22 tests, 0 failures, incl. `migrate17To18_preservesAccountsAndRoundTripsHoldingsAndLotsWithoutStoringAValue` and `currentSchemaMatchesItsExportedFixture` |
| 2026-08-25 | 7 | `./gradlew :app:connectedDebugAndroidTest` | FAIL then OK — `CfoSmokeTest.theRecurringSectionProposesASeriesFromTheRealLedger` timed out at 20s once, passed on re-run (9/9). Pre-existing test, unrelated to 6.3; **flaky on a headless swiftshader emulator** |
| 2026-08-25 | 7 | `./gradlew :feature:onboarding:connectedDebugAndroidTest` | OK — 1 test, 0 failures |
| 2026-08-25 | 7 | `./gradlew :app:installDebug` + cold start on a cleared app | OK — no crash. An earlier SEC-002 crash was reproduced against a **stashed, clean `dev` baseline** and did not recur on either build; it was fallout from a SystemUI ANR during the first boot, not 6.3 |
| 2026-08-25 | 7 | `/run` + `/verify` — holding created by hand in demo mode | OK — PPFAS Flexi Cap, equity, ₹82.50/unit priced 2027-01-01, one BUY of 100 units for ₹7,500 on 2026-01-01. Screen shows **₹8,250.00**, **₹7,500.00 invested**, **₹750.00 gain**, **10.0% a year** — the golden file's hand-checkable case, end to end through SQLCipher |
| 2026-08-25 | 7 | Empty-state and prompt (P-03) | OK — an investment account with nothing shows "Add what this account holds", never ₹0; the holdings screen shows its invitation |
| 2026-08-25 | 7 | §11.1 disclaimer (P-07) | OK — on screen on the holdings screen, list and editor alike |
| 2026-08-25 | 7 | Airplane mode (P-04) | OK — `airplane_mode_on=1`, app force-stopped and relaunched: every figure renders identically |
| 2026-08-25 | 7 | Privacy blur (issue 5.3) | OK — "₹••••••• across 1 holding" on the accounts row; invested/gain/value masked on the holdings screen. **The rate is deliberately not masked** — a percentage says nothing about how much money is involved |
| 2026-08-25 | 5 | `./gradlew verifyPaparazziDebug` | OK — no screenshot diffs |
| 2026-08-25 | 5 | `./gradlew -p build-logic :convention:test` | OK |
| 2026-08-25 | 5 | `ai/**/*.json` parse + rulebook version | OK — 8 files parse; `_meta.version` still `1.13.0`, 32 rules, 8 AI-INV rows untouched |
| 2026-08-25 | 7 | `/run` — `emulator -avd CfoTest -no-window`, `./gradlew installDebug`, `am start .../MainActivity` | OK — installed on 1 device, pid 3506, app focused, crash buffer empty, dashboard rendered. SystemUI ANR'd on first boot (emulator, not the app) and was dismissed; `screencap` returns 0 bytes afterwards on this headless swiftshader AVD, so the UI hierarchy dump is the evidence of what rendered |
| 2026-08-25 | 7 | `/verify` step 2 — holdings screen after an emulator reboot **and** an app reinstall | OK — PPFAS Flexi Cap, Equity, ₹8,250.00, ₹7,500.00 invested, ₹750.00 gain, 10.0% a year. The row survived both, so the SQLCipher round trip is proven end to end |
| 2026-08-25 | 7 | `/verify` step 2b — P-03 prompt vs zero, both on one screen | OK — **Index Fund Folio** "₹8,250.00 across 1 holding" (ICU plural); **Zerodhaz**, which has none, "Add what this account holds" — never "₹0.00 across 0 holdings" |
| 2026-08-25 | 7 | `/verify` step 3 — numbers from math (P-03), inputs beside the answer (P-02) | OK — 100 x ₹82.50 = ₹8,250; 8250 − 7500 = ₹750; 8250/7500 over exactly 365 days = +10.0%. Matches the golden fixture's `simple-year` block (`expect_bps=1000`) reproduced through the UI |
| 2026-08-25 | 7 | `/verify` step 3c — §11.1 disclaimer (P-07) | OK — rendered on the list and with the editor open |
| 2026-08-25 | 7 | `/verify` step 4 — airplane mode, force-stop, cold relaunch | OK — `airplane_mode_on=1`; every figure identical offline |
| 2026-08-25 | 7 | `/verify` step 5 — edge path: a unit price with no date | OK — save **refused**, editor stayed open, pid unchanged, crash buffer empty, and the holdings list still held only PPFAS. The both-or-neither invariant holds on a device, not just in a unit test |
| 2026-08-25 | 7 | `/verify` step 6 — `./gradlew connectedDebugAndroidTest` (`:core:database`, `:feature:onboarding`, `:app`) | OK — **32 instrumented tests, 0 failures**. `:app` needed a second pass: the first was killed with the emulator mid-task, and the earlier 9/9 was from a prior session, so it was re-run rather than carried over |
| 2026-08-25 | 9 | `git commit` / `git push` | **not run** — CLAUDE.md §7 and workflow step 12: do not commit or push unless asked |
