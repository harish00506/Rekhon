# [8.3] Backup restore drill

**Status:** Done — merged to `dev` (0.8.3); Epic 8 complete  
**Epic:** Epic 8 — Backup & Restore  
**Priority:** Medium  
**Depends on:** 8.2  
**Assignee:** Harish G  
**Labels:** `backup`, `testing`

---

## Description

An automated restore drill in the instrumented tests proving backups are actually restorable (guards against silent backup rot).

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §21.5, §34.4  
**Design spec section:** §10 Testing  
**Epic summary:** End-to-end-encrypted backup, restore on a fresh device, and an automated restore drill so backups are proven, not assumed.

**Branch:** `feature/8-3-backup-restore-drill`

## Tracker

Progress and phase checklist: [8.3-backup-restore-drill-tracker.md](8.3-backup-restore-drill-tracker.md)

---

## Acceptance Criteria

- [x] The drill generates a backup on a seeded DB, restores on a clean instance, and asserts row parity. — every one of 22 profile-scoped tables seeded; parity read from `sqlite_master`, counts and contents; the clean instance is a fresh SQLCipher DB with its key deleted.
- [x] It runs as an instrumented test; failure blocks release. — `BackupRestoreDrillDeviceTest` via `./gradlew restoreDrill` (exit 1 on a seeded regression); CI `restore-drill` job on PRs to `stage`/`main`. **Blocking in CI needs the job listed as a required check in branch protection** — a GitHub setting, unverified from here (ADR-0041).
- [x] It is documented as a recurring release-gate step. — `00-issue-workflow.md` "Release gate", `CLAUDE.md` §7, `/pre-merge` item 11.
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched. — a single changed paisa fails the drill; no network path.
- [x] All Skill Rules and label-specific rules below are satisfied. — proven restorable, not assumed; both drills watched go red.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`). — see the tracker.

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `keystore-crypto` | Tink/Keystore only (SEC-003): SQLCipher key, E2EE backup (Argon2id+AES-GCM), crypto-shred erase. | `~/.claude/skills/keystore-crypto/SKILL.md` |
| `security-review` | Mandatory for crypto, keys, SMS, backup, or consent changes (§23, SEC-*). | `~/.claude/skills/security-review/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `paparazzi-screenshot-testing` | Compose screenshot tests in light/dark/200% from a fixed UiState; baselines gate merges (§21.5). | `~/.claude/skills/paparazzi-screenshot-testing/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **SEC-003 / P-01** - Tink/Keystore only (no hand-rolled crypto); every off-device path is consent-gated.

---

## Workflow Rules — Before Starting

- [x] Dependencies (8.2) are complete or explicitly waived by the user
- [x] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [x] Read the SRS sections cited above (§21.5, §34.4) and quote the requirement IDs back
- [x] Read the design spec section: **§10 Testing**
- [x] Load the skill files listed above for this issue's labels
- [x] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Backups are proven restorable, not assumed (§21.5).

### Label-specific

1. Fail-secure on a bad passphrase; restore is atomic; off-device writes are consent-gated (P-01).
2. Golden-file + property + seeded-determinism tests; engine coverage ≥ 85%, money math 100% (§21.5).

### Epic-specific

1. Backups are E2E-encrypted with Tink and proven restorable (SEC-003/§21.5).

---

## Workflow Rules — Definition of Done

- [x] Acceptance criteria checked off above
- [x] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [x] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [x] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [x] No secrets/PII/amounts in logs or committed files
- [x] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [x] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [x] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

Done 2026-09-19 (full log in the tracker). ADR-0041 records the relation to DRL-001's in-app drill
(deferred) and what the CI gate cannot yet enforce.

```text
./gradlew unitTests koverVerify ktlintCheck detekt lintDebug :app:assembleRelease :app:assembleDebug
./gradlew restoreDrill        # on CfoTest (API 36): 3/3 incl. the drill
Mutation (insertGoals removed from the restore): JVM drill red, device drill red, restoreDrill exit 1.
```

---

## Files Changed

| Path | Action |
|------|--------|
| `data/repository/src/sharedTest/.../DrillFixture.kt`, `DatabaseParity.kt` | Added |
| `data/repository/src/test/.../BackupRestoreDrillTest.kt` | Added — JVM drill (4) |
| `data/repository/src/androidTest/.../BackupRestoreDrillDeviceTest.kt` | Added — release-gate drill |
| `data/repository/build.gradle.kts`, `build.gradle.kts` | Modified — `sharedTest`; `restoreDrill` |
| `.github/workflows/ci.yml` | Modified — `restore-drill` job |
| `docs/issues/00-issue-workflow.md`, `CLAUDE.md`, `.claude/commands/pre-merge.md` | Modified — release gate |
| `docs/adr/0041-…`, `DECISIONS.md`, `docs/sessions/2026-09-19-backup-restore-drill.md` | Added / Modified |
| `VERSION`, `app/build.gradle.kts`, `CHANGELOG.md`, `docs/memory.md`, this issue + tracker | Modified |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/8.3-backup-restore-drill/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
