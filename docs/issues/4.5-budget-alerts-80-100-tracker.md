# Issue 4.5 — Budget alerts (80%/100%) Tracker

**Parent issue:** [4.5-budget-alerts-80-100.md](4.5-budget-alerts-80-100.md)  
**Strategy:** Threshold alerts at 80% and 100% of a budget, with the thresholds as versioned rule rows and the notification gated by the guardrail.  
**Branch (integration):** `dev`  
**Branch (implementation):** `feature/4-5-budget-alerts-80-100` → merged to `dev`

---

## Status Summary

| Field | Value |
|-------|-------|
| **Status** | In progress — code complete, JVM gates green; phase 7 (device) and 9 (merge) outstanding |
| **Progress** | 7 / 9 phases |
| **Last verified** | 2026-08-13 |
| **VERSION at start** | 0.4.3 → 0.4.4 at ship |
| **Depends on** | 4.4 |
| **Branch** | `feature/4-5-budget-alerts-80-100` |

---

## Phase Checklist

| # | Phase | Status | Verification |
|---|-------|--------|--------------|
| 1 | Dependencies clear or waived | [x] | 4.4 shipped at `f3f4228` (`RULE-BUD-PACE`, `:domain:engines:budget`, `:feature:budgets`) |
| 2 | Branch created | [x] | `feature/4-5-budget-alerts-80-100`, off the 4.4 state |
| 3 | Failing tests written (TDD) | [x] | drift, boundary, claim-once and worker-ordering tests written first; two gates additionally **watched to fail on purpose** (log below) |
| 4 | Implementation to acceptance criteria | [x] | five commits, `9debbd2` → `f34abeb`; every criterion but the device run ticked in the issue |
| 5 | Static analysis clean | [x] | `./gradlew ktlintCheck detekt lintDebug` — OK |
| 6 | Unit + coverage gate | [x] | `./gradlew unitTests koverVerify` — OK |
| 7 | Run on emulator (real gate) | [ ] | **not done** — `/run` + `/verify` and `:core:database:connectedDebugAndroidTest` both need a device; the 6-step script is in the plan and repeated below |
| 8 | Docs + VERSION + CHANGELOG | [x] | ADR-0019, `ENGINE.md`, `VERSION` 0.4.4, `CHANGELOG.md` |
| 9 | Merge to `dev` + push | [ ] | blocked on phase 7 — nothing pushed, no PR opened |

**Phase 7 is the honest gap.** Six things are unobserved, and a green JVM build cannot stand in for
any of them: (1) crossing 80% posts exactly one notification; (2) crossing 100% posts a second;
(3) further spending posts no third; (4) denying the permission leaves the banner correct and the
app alive; (5) all of the above in airplane mode (P-04); (6) dark mode and 200% font, with the
overspend **not** drawn in the money-in green — the exact defect the 4.4 device run found (`0e6aa41`).
The 13 → 14 migration round-trip, including the `SQLiteConstraintException` that enforces
once-per-band, has likewise only been written, never executed.

---

## Verification Log

Append one row per verify step (lint, unit, coverage, screenshot, emulator run, E2E, git push).
Use `OK`, `SKIP - <reason>`, or `FAIL - <reason>`. `SKIP` must name why (never to go green).

| Date & time (UTC+5:30) | Phase | Command | Result |
|------------------------|-------|---------|--------|
| 2026-08-13 | 3 | `unitTests` with `warn_pct` 80 → 75 in `rules-kb.json` **only** | OK (red as intended) - `RulebookDriftTest > the alert thresholds match RULE-BUD-ALERT` failed. An `ai/`-only edit going red is what proves `inputs.file` is wired; reverted |
| 2026-08-13 | 3 | `unitTests` with `"warn_pct"` added to `RULE-BUD-PACE.params_json` | OK (red as intended) - the retargeted tripwire `the alert bands live on RULE-BUD-ALERT, not RULE-BUD-PACE` failed; reverted |
| 2026-08-13 | 3 | `:domain:engines:budget:test` with `exceeded_pct` moved below 100 | FAIL - real defect, not a gate: `overspentBy` went negative when the band was crossed with budget left. Fixed by flooring at zero (ADR-0019); test now asserts `Money.ZERO` |
| 2026-08-13 | 5 | `./gradlew ktlintCheck detekt lintDebug` | OK - clean. Three detekt findings were fixed by extraction (`BudgetAlertBands`, `BudgetPace`, `NotificationPermissionRequest`, `compose`), one by an argued `@Suppress` on `RoomBudgetRepository`; the Android lint `MissingPermission` error was fixed by **inlining** the permission check rather than suppressing it |
| 2026-08-13 | 6 | `./gradlew unitTests` | OK - all suites green, including 20 golden records (was 12) |
| 2026-08-13 | 6 | `./gradlew koverVerify` | OK - EXIT=0; engine ≥ 85%, money math 100% |
| 2026-08-13 | 6 | `:core:model:test` (`NumericGuardrailTest`) | OK - 10 cases; the fail-closed default and GRD-003 (correct arithmetic the engine never produced) both refuse |
| 2026-08-15 | 7 | `/run` on `CfoTest` (AVD, API 34): `installDebug` + launch | OK - build installed and launched. First cold launch (fresh install, no PIN ever configured) crashed once: `IllegalStateException` "the encrypted database was opened while the app was locked (SEC-002)" from `CoreModule.provideDatabase`. Root cause: `AppLockViewModel.toUiState`'s `!stored.enabled -> LockStatus.UNLOCKED` branch opens content the instant the "disabled" setting is read, independent of `SessionLock.isUnlocked`; the matching `sessionLock.unlock()` call lives in a **separate** `init{}` coroutine racing the same `appLockStore.observe()` flow, with no happens-before edge between them. Did not reproduce on 3 immediate relaunches (timing-dependent). This is issue 2.2 code, not 4.5's diff, but it blocked the first attempt at this run. **Not fixed here** — out of 4.5's scope; filed as [2.8](2.8-app-lock-disabled-unlock-race-sec-002-crash.md) |
| 2026-08-15 | 7 | `cmd jobscheduler run -f` / `get-job-state` against `BudgetAlertWorker`'s scheduled job, incl. with `adb root` | SKIP - confirmed a device/tooling limitation, not an app defect: even a read-only `get-job-state` call returns `unknown(u0a192/jidNN)` / "Could not find job" for WorkManager `SystemJobScheduler` jobs on this AVD (API 34), for every job id observed across two emulator sessions. The 24h `PeriodicWorkRequest` interval (`ExistingPeriodicWorkPolicy.KEEP`) means the actual OS notification post for `BudgetAlertWorker` remains unobserved this session |
| 2026-08-15 | 7 | Set budget ₹300 on Groceries (₹250 already spent = 83%) | OK - in-app banner "1 category has used 80% or more of its budget" and per-row "80% used / ₹50.00 left of ₹300.00" rendered correctly (P-02); `POST_NOTIFICATIONS` prompt fired contextually right after save, as designed |
| 2026-08-15 | 7 | Deny the notification permission | OK - app stayed alive, no crash; `dumpsys notification` confirmed `importance=NONE userSet=true`; the in-app banner/chip stayed correct after denial, confirming the band is derived from `observeBudgets()` and not the notification path |
| 2026-08-15 | 7 | Add ₹80 expense to Groceries (₹330 / ₹300 = 110%) | OK - banner flipped to "1 category is over budget this month", row shows "Over budget" / "-₹30.00 left of ₹300.00" in the error colour, never money-in green |
| 2026-08-15 | 7 | Dark mode + 200% font on the Budgets screen | OK - banner and card re-rendered with no clipping/overlap; overspend text stayed in the error/salmon colour, not green - the exact class of defect 4.4's device run caught |
| 2026-08-15 | 7 | Actual OS notification post (`BudgetAlertWorker` → `NotificationManager`) | SKIP - could not force the periodic WorkManager job to run on demand in this session (`adb shell cmd jobscheduler run -f` repeatedly reported "Could not find job", a tooling/namespace quirk against `SystemJobScheduler`-backed WorkManager jobs, not an app defect). The claim-before-notify and guardrail-gated compose logic remain verified only at the unit/integration level, not observed as a real posted notification on this device |
| 2026-08-15 | 7 | Airplane mode | OK - airplane mode was on for the entire session (visible in every screenshot); budget save, transaction add, banner rendering and permission handling all worked with it on throughout (P-04) |
| 2026-08-15 | 7 | `./gradlew :core:database:connectedDebugAndroidTest` | OK - 18/18 instrumented tests passed on `CfoTest`, including the 13 → 14 migration round-trip and its `SQLiteConstraintException` once-per-band assertion |
| - | 9 | `git push` / PR to `dev` | SKIP - not attempted; the user did not ask for a push, and the notification-post leg above is still unobserved |
