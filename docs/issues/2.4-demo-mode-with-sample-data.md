# [2.4] Demo mode with sample data

**Status:** Implemented — verified on an emulator; awaiting commit/merge  
**Epic:** Epic 2 — Onboarding, Security & Accounts  
**Priority:** Medium  
**Depends on:** 2.1  
**Assignee:** Harish G  
**Labels:** `onboarding`

---

## Description

A demo mode that loads a labelled sample dataset so the app can be explored without entering real data - clearly marked and fully wipeable.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §5.1 (**FR-ONB-004**) — the “§33 (demo)” citation in the generator was wrong (§33 is the Growth Optimizer Suite); fixed at `scripts/gen_issue_docs.py`  
**Design spec section:** §6 Module Structure  
**Epic summary:** First-run onboarding, biometric app lock, and the accounts + net-worth core - the entry point and the entities balances are computed from.

**Branch:** `feature/2-4-demo-mode-with-sample-data`

## Tracker

Progress and phase checklist: [2.4-demo-mode-with-sample-data-tracker.md](2.4-demo-mode-with-sample-data-tracker.md)

---

## Acceptance Criteria

- [x] Demo loads a fixed sample dataset into an isolated/flagged profile, clearly labelled as demo.
- [x] Exiting demo wipes the sample data with no residue in the real profile.
- [x] The dataset is deterministic (seeded) and covered by a test.
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched.
- [x] All Skill Rules and label-specific rules below are satisfied.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`).

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `compose-ui` | Compose screens: immutable UiState/StateFlow (ARC-004), M3 tokens, strings.xml/ICU, a11y, Paparazzi. | `~/.claude/skills/compose-ui/SKILL.md` |
| `source-driven-development` | Ground framework/library use (Compose, Room, Hilt, ML Kit) in official docs before coding. | `~/.claude/skills/source-driven-development/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **§21.6 theming/i18n** - strings in `strings.xml` (ICU plurals); colours/dimensions from theme tokens.

---

## Workflow Rules — Before Starting

- [x] Dependencies (2.1) are complete or explicitly waived by the user
- [x] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [x] Read the SRS sections cited above (§5.1, §33 (demo)) and quote the requirement IDs back
- [x] Read the design spec section: **§6 Module Structure**
- [x] Load the skill files listed above for this issue's labels
- [x] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Demo data is visibly labelled and isolated from real data (P-02).

### Label-specific

1. Consent is captured before any optional data path is enabled (P-01).

### Epic-specific

1. Everything works offline; consent is captured before any optional feature is enabled (P-01/P-04).

---

## Workflow Rules — Definition of Done

- [x] Acceptance criteria checked off above
- [x] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [x] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [x] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [x] No secrets/PII/amounts in logs or committed files
- [x] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [x] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [x] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

**Done.** All gates run; the emulator gate is no longer blocked.

```text
./gradlew ktlintCheck detekt lintDebug     OK - no new warnings (60 pre-existing dependency notices)
./gradlew testDebugUnitTest koverVerify    OK - 587 passed, 0 skipped
./gradlew verifyPaparazziDebug             OK - CfoDemoBanner in the gallery, light/dark/200%
emulator -avd CfoTest + :app:installDebug  OK - first emulator run in this project's history
  onboarding -> demo -> banner -> exit     OK - observed by hand, screenshots in the session
  same flow with airplane mode on          OK - identical behaviour (P-04)
./gradlew connectedDebugAndroidTest        OK - on emulator-5554
```

Both new gates were **proven to fail before being trusted**: a one-digit seed change reddened the
golden dataset test, and swapping one hard delete for a soft delete reddened the residue test.

Full timestamped log: [2.4-demo-mode-with-sample-data-tracker.md](2.4-demo-mode-with-sample-data-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `core/datastore/src/main/proto/cfo_settings.proto` | Modified - `demo_mode_active` (field 16) |
| `core/datastore/.../SettingsStore.kt`, `Model.kt` | Modified - the flag's setter and snapshot field |
| `core/database/.../dao/Daos.kt` | Modified - `DemoDao`, the app's only hard-delete DAO |
| `core/database/.../CfoDatabase.kt` | Modified - `demoDao()`; **VERSION unchanged (3)** |
| `data/repository/.../DemoDataset.kt` | **New** - the deterministic seeded fixture |
| `data/repository/.../DemoModeRepository.kt` | **New** - enter / exit / active profile |
| `data/repository/.../QuickSetupRepository.kt` | Modified - reads follow the active profile |
| `data/repository/.../RepositoryFactory.kt`, `build.gradle.kts` | Modified - the new binding + `:core:datastore` |
| `core/designsystem/.../component/CfoDemoBanner.kt` | **New** - the label every screen carries |
| `feature/onboarding/.../OnboardingWriter.kt` | **New** - consent + profile writes, extracted |
| `feature/onboarding/.../OnboardingViewModel.kt`, `OnboardingSteps.kt`, `OnboardingScreen.kt`, `OnboardingUiState.kt` | Modified - the demo entry point |
| `app/.../di/RepositoryModule.kt` | **New** - repository bindings split out of `CoreModule` |
| `app/.../MainActivity.kt`, `MainViewModel.kt`, `navigation/CfoNavHost.kt` | Modified - the banner, the exit, the start destination |
| `app/`, `feature/onboarding/` `strings.xml` | Modified - all new copy externalised (S21.6) |
| `feature/onboarding/src/androidTest/.../OnboardingFlowInstrumentedTest.kt` | Modified - **repaired**; had not compiled since 2.3 |
| `docs/adr/0006-demo-mode-profile-isolation-and-hard-delete.md` | **New** |
| `scripts/gen_issue_docs.py` | Modified - the wrong SRS citation, fixed at the source |
| Tests: `DemoDatasetTest`, `DemoModeRepositoryTest`, `FakeSettingsStore` (+ additions to 5 existing suites) | **New / modified** |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/2.4-demo-mode-with-sample-data/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
