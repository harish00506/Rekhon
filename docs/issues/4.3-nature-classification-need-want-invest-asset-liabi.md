# [4.3] Nature classification (Need/Want/Invest/Asset/Liability)

**Status:** Todo  
**Epic:** Epic 4 — Categorisation & Budgets  
**Priority:** High  
**Depends on:** 4.2  
**Assignee:** Harish G  
**Labels:** `categorisation`, `ai`

---

## Description

Classify each transaction's financial nature (Need / Want / Invest / Asset / Liability) - the signal Safe-to-Spend, the health score, and advice all build on.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §8, §14; AI-CLS  
**Design spec section:** §4 AI Architecture  
**Epic summary:** The category KB, Stage-1 auto-categorisation, Need/Want/Invest nature classification, and budgets with threshold alerts.

**Branch:** `feature/4-3-nature-classification-need-want-invest-asset-liabi`

## Tracker

Progress and phase checklist: [4.3-nature-classification-need-want-invest-asset-liabi-tracker.md](4.3-nature-classification-need-want-invest-asset-liabi-tracker.md)

---

## Acceptance Criteria

- [x] Deterministic nature assignment with evidence; user-overridable (the override is stored as data).
- [x] Feeds Safe-to-Spend and health-score inputs with a consistent taxonomy (§14) — §8.3's true-spend
      breakdown, rendered on the dashboard.
- [x] Golden-file test over a labelled fixture — 21 records fixing the nature **and the cited rule**.
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched.
- [x] All Skill Rules and label-specific rules below are satisfied.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`).

**Scope note:** §8.3.1 is six steps, and three of them name tables this codebase does not have
(`loan_amortization_rows`, goals, holdings). Steps 1-3 fire on the account's **type** instead, step 6
becomes a confidence flag rather than a prompt, and `trueSpend` ships without its interest term — all
recorded in [ADR-0016](../adr/0016-nature-classification-by-account-type.md).

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `add-rulebook-rule` | Add/change any financial threshold as a versioned data row in ai/rules/ (never hardcoded). | `.claude/skills/add-rulebook-rule/SKILL.md` |
| `new-engine` | Scaffold a :domain:engines:* module (one interface, provenance, Hilt, ENGINE.md, tests). | `.claude/skills/new-engine/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `source-driven-development` | Ground framework/library use (Compose, Room, Hilt, ML Kit) in official docs before coding. | `~/.claude/skills/source-driven-development/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **AI-ARC-003/006** - every engine result carries provenance (engineId/version/inputWindow/computedAt/confidence/evidence).

---

## Workflow Rules — Before Starting

- [ ] Dependencies (4.2) are complete or explicitly waived by the user
- [ ] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [ ] Read the SRS sections cited above (§8, §14; AI-CLS) and quote the requirement IDs back
- [ ] Read the design spec section: **§4 AI Architecture**
- [ ] Load the skill files listed above for this issue's labels
- [ ] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Deterministic + explainable (P-08/P-02); overrides are data.

### Label-specific

1. Merchant/category rules are versioned data rows cited by ID, not code (§29/AI-ARC-006).
2. The LLM never computes a figure - it verbalises engine output, gated by the guardrail (P-03/AI-ARC-004).

### Epic-specific

1. Categorisation and budget thresholds are versioned data rows cited by ID - never hardcoded (§29).

---

## Workflow Rules — Definition of Done

- [ ] Acceptance criteria checked off above
- [ ] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [ ] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [ ] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [ ] No secrets/PII/amounts in logs or committed files
- [ ] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [ ] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [ ] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

All green on 2026-08-10:

```text
./gradlew ktlintCheck detekt lintDebug            # clean
./gradlew unitTests koverVerify                   # green; engine >= 85%
./gradlew :core:database:connectedDebugAndroidTest  # 17/17, incl. the new 12 -> 13 migration
# emulator CfoTest, radios off, installed over a real v12 database:
#   dashboard -> "This month, actually - Needs Rs349 / Wants Rs150 / Kept Rs0"
#   detail sheet -> "Decided by rule CLS-NAT-005"; tap Want -> CLS-NAT-004; tap again -> back to 005
```

Full timestamped log: [4.3-nature-classification-need-want-invest-asset-liabi-tracker.md](4.3-nature-classification-need-want-invest-asset-liabi-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `ai/knowledge/classification-kb.json` | v1.2 -> **v1.3**: `CLS-NAT-001..006` ids + versions, new `stage_nature` block |
| `domain/engines/nature/**` | **New module** - engine, rules mirror, breakdown fold, ENGINE.md, 4 test classes, golden file |
| `settings.gradle.kts` | includes `:domain:engines:nature` |
| `core/database/.../entity/Entities.kt` | `TransactionEntity.nature` |
| `core/database/.../migration/Migrations.kt` | `MIGRATION_12_13`, additive `ADD COLUMN`, no backfill |
| `core/database/schemas/.../13.json` | exported |
| `core/database/src/androidTest/.../MigrationRoundTripTest.kt` | the 12 -> 13 case |
| `core/database/.../dao/Daos.kt` | 5 queries + 4 projections for §8.3.1's joins |
| `data/repository/.../TransactionRepository.kt` | `natureOf`, `setNature`, `observeNatureBreakdown` |
| `data/repository/.../TransactionNature.kt` | **New** - the mappers those three need |
| `data/repository/src/test/.../NatureRepositoryTest.kt` | **New** - the joins, against in-memory Room |
| `feature/transactions/.../TransactionNatureUi.kt` | **New** - the detail sheet's nature section |
| `feature/transactions/src/test/.../TransactionNatureFlowTest.kt` | **New** - its Compose tests |
| `feature/dashboard/**` | the "This month, actually" line and its caveat |
| `docs/adr/0016-...md` | **New** - account-type signals, override-only storage, understated true spend |
| `VERSION`, `CHANGELOG.md` | `0.4.1` -> `0.4.2` |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/4.3-nature-classification-need-want-invest-asset-liabi/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
