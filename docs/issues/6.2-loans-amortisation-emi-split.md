# [6.2] Loans + amortisation + EMI split

**Status:** Done  
**Epic:** Epic 6 — Wealth: Loans & Investments  
**Priority:** High  
**Depends on:** 2.5, 3.3  
**Assignee:** Harish G  
**Labels:** `wealth`, `loans`, `money`

---

## Description

Loan accounts with a deterministic amortisation schedule and an EMI principal/interest split - exact paise math, rates in basis points.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §5.8, §11; MNY-001, MNY-002  
**Design spec section:** §7 Money & Time Rules  
**Epic summary:** Credit cards, loans with amortisation, investment holdings with XIRR, allocation, and net-worth history - exact paise math throughout.

**Branch:** `feature/6-2-loans-amortisation-emi-split`

## Tracker

Progress and phase checklist: [6.2-loans-amortisation-emi-split-tracker.md](6.2-loans-amortisation-emi-split-tracker.md)

---

## Acceptance Criteria

- [x] The amortisation schedule is computed deterministically; each EMI splits into principal/interest exactly (sum = EMI, property test).
- [x] Rates are bps and amounts are `Money` paise; HALF_EVEN rounding with remainder handling.
- [x] Golden-file test vs a known schedule; 100% money-math coverage.
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched.
- [x] All Skill Rules and label-specific rules below are satisfied.
- [x] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`).

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `api-and-interface-design` | Design the one public interface + result types before implementing. | `~/.claude/skills/api-and-interface-design/SKILL.md` |
| `money-time-audit` | Scan the diff for the Money/Clock bug factories before merge. | `.claude/skills/money-time-audit/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **MNY-001/002** - Money is `Long` paise; rates are integer basis points; no `Double`/`Float` on money.

---

## Workflow Rules — Before Starting

- [x] Dependencies (2.5, 3.3) are complete or explicitly waived by the user
- [x] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [x] Read the SRS sections cited above (§5.8, §11; MNY-001, MNY-002) and quote the requirement IDs back
- [x] Read the design spec section: **§7 Money & Time Rules**
- [x] Load the skill files listed above for this issue's labels
- [x] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. 100% money-math coverage; rates bps, amounts paise (MNY-001/002).

### Label-specific

1. Money is `Long` paise end-to-end; no `Double`/`Float` on a monetary value (MNY-001).
2. Splits distribute the remainder (largest-remainder, `Money.split`/`allocate`) so parts sum exactly; HALF_EVEN is for rounding a single value (`Money.percentOf`), and cannot make N parts sum to a whole.

### Epic-specific

1. Wealth math is exact: `Money` paise, rates in bps, 100% money-math coverage (MNY-001/002).

---

## Workflow Rules — Definition of Done

- [x] Acceptance criteria checked off above
- [x] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [x] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [x] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [x] No secrets/PII/amounts in logs or committed files
- [x] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [x] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [x] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

All green on 2026-08-20. Commands, results and the seven emulator scenarios are logged in the
tracker:

```text
./gradlew ktlintCheck detekt lintDebug                 BUILD SUCCESSFUL (whole repo)
./gradlew unitTests koverVerify                        BUILD SUCCESSFUL
./gradlew :feature:accounts:compileDebugAndroidTestKotlin verifyPaparazziDebug   SUCCESSFUL
./gradlew :core:database:connectedDebugAndroidTest     21 tests on CfoTest(AVD) - 14, passed
```

On the emulator the derived EMI for **₹30,00,000 · 8.5% · 240 months** reads **₹26,034.70**,
split **₹4,784.70 principal · ₹21,250.00 interest** — the golden file's figure, on the device.

Two gates were broken on purpose and watched go red before being trusted: the closing
instalment's remainder absorption (5 tests red) and widening `showsLoanFields` to
`type.isLiability` (2 red).

Full timestamped log: [6.2-loans-amortisation-emi-split-tracker.md](6.2-loans-amortisation-emi-split-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `core/model/src/main/kotlin/com/aicfo/core/model/Loan.kt` | Added — the five terms a schedule is a pure function of |
| `core/model/src/main/kotlin/com/aicfo/core/model/Money.kt` | Changed — `percentOf(bps, overPeriods = 1)` for a monthly slice of an annual rate |
| `core/model/src/test/kotlin/com/aicfo/core/model/LoanTest.kt` | Added — one case per `require`, both boundaries |
| `core/model/src/test/kotlin/com/aicfo/core/model/MoneyTest.kt` | Changed — four `overPeriods` cases incl. the tie-to-even |
| `core/database/src/main/kotlin/com/aicfo/core/database/entity/Entities.kt` | Changed — `LoanEntity` (table `loan`) |
| `core/database/src/main/kotlin/com/aicfo/core/database/dao/Daos.kt` | Changed — `LoanDao`; `deleteLoans`; `loans`/`insertLoans` |
| `core/database/src/main/kotlin/com/aicfo/core/database/CfoDatabase.kt` | Changed — `VERSION = 17`, `loanDao()` |
| `core/database/src/main/kotlin/com/aicfo/core/database/migration/Migrations.kt` | Changed — `MIGRATION_16_17`, additive (DB-003) |
| `core/database/schemas/com.aicfo.core.database.CfoDatabase/17.json` | Added — exported schema, committed |
| `core/database/src/androidTest/.../MigrationRoundTripTest.kt` | Changed — 16→17 round trip; asserts no schedule table exists |
| `settings.gradle.kts` | Changed — `include(":domain:engines:loan")` |
| `domain/engines/loan/build.gradle.kts` | Added — pure-Kotlin engine module (ARC-002), no rulebook input |
| `domain/engines/loan/src/main/kotlin/.../LoanEngine.kt` | Added — the one public interface, result types, factory (ARC-003) |
| `domain/engines/loan/src/main/kotlin/.../Emi.kt` | Added — closed form at a pinned `MathContext(34, HALF_EVEN)`; zero-rate branch; amortises guard |
| `domain/engines/loan/src/main/kotlin/.../Amortisation.kt` | Added — the reducing-balance walk; closing instalment absorbs the remainder |
| `domain/engines/loan/src/main/kotlin/.../DefaultLoanEngine.kt` | Added — override-over-derived, every `Err` path, provenance |
| `domain/engines/loan/ENGINE.md` | Added — contract, formulas, assumptions, version log |
| `domain/engines/loan/src/test/kotlin/.../LoanGoldenTest.kt` + `resources/golden/loan.txt` | Added — 5 loans, ~500 instalments, from an independent decimal implementation |
| `domain/engines/loan/src/test/kotlin/.../LoanEnginePropertyTest.kt` | Added — 500 seeded loans against five identities |
| `domain/engines/loan/src/test/kotlin/.../EmiTest.kt`, `LoanEngineTest.kt` | Added — the closed form and every refusal |
| `data/repository/src/main/kotlin/.../LoanRepository.kt` | Added — the only class that touches `LoanDao` (ARC-005) |
| `data/repository/src/main/kotlin/.../RepositoryFactory.kt` | Changed — `loans(...)` |
| `data/repository/src/main/kotlin/.../Archive.kt`, `ArchiveRepository.kt`, `DemoModeRepository.kt` | Changed — `loan` in export, import and the demo wipe |
| `data/repository/build.gradle.kts` | Changed — `api(project(":domain:engines:loan"))` |
| `data/repository/src/test/kotlin/.../LoanRepositoryTest.kt` | Added — 12 cases incl. four points in a loan's life |
| `data/repository/src/test/kotlin/.../ArchiveRepositoryTest.kt` | Changed — `loan` in the round-trip snapshot |
| `app/src/main/kotlin/com/aicfo/app/di/WealthEngineModule.kt` | Added — Epic 6's card and loan engines |
| `app/src/main/kotlin/com/aicfo/app/di/EngineModule.kt` | Changed — the two wealth engines moved out |
| `app/src/main/kotlin/com/aicfo/app/di/RepositoryModule.kt` | Changed — `provideLoanRepository` |
| `app/build.gradle.kts` | Changed — `versionCode = 24` |
| `feature/accounts/src/main/kotlin/.../AccountsUiState.kt` | Changed — `loans` map, loan fields, `showsLoanFields`, `LoanField` |
| `feature/accounts/src/main/kotlin/.../AccountEditorLoanFields.kt` | Added — the loan section of the editor |
| `feature/accounts/src/main/kotlin/.../AccountEditorViewModel.kt` | Changed — `saveLoanTerms`, `toLoan`, `parseRateBps`/`formatRatePercent` |
| `feature/accounts/src/main/kotlin/.../AccountEditorScreen.kt` | Changed — the loan branch beside the card one |
| `feature/accounts/src/main/kotlin/.../AccountsViewModel.kt`, `AccountsScreen.kt` | Changed — `observeLoans()`; the next-EMI split on a loan row |
| `feature/accounts/src/main/res/values/strings.xml` | Changed — the loan copy; the validation message now covers every field |
| `feature/accounts/src/test/kotlin/.../AccountEditorLoanFieldsTest.kt`, `FakeLoanRepository.kt` | Added |
| `feature/accounts/src/test/kotlin/.../AccountEditorViewModelTest.kt`, `AccountsViewModelTest.kt`, `ReconcileViewModelTest.kt` | Changed — loan cases and the new dependency |
| `feature/accounts/build.gradle.kts` | Changed — the Compose test excluded from the Release variant |
| `docs/adr/0026-amortisation-schedule-is-derived-not-stored.md` | Added |
| `DECISIONS.md`, `FLOW.md`, `docs/sessions/2026-08-20-loans-amortisation-emi-split.md` | Changed / added (§10) |
| `VERSION`, `CHANGELOG.md` | Changed — 0.6.2 |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/6.2-loans-amortisation-emi-split/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
