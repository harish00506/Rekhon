# [4.5] Budget alerts (80%/100%)

**Status:** In progress — code complete and green on the JVM; the emulator run (phase 7) is outstanding  
**Epic:** Epic 4 — Categorisation & Budgets  
**Priority:** High  
**Depends on:** 4.4  
**Assignee:** Harish G  
**Labels:** `budgets`, `notifications`

---

## Description

Threshold alerts at 80% and 100% of a budget, with the thresholds as versioned rule rows and the notification gated by the guardrail.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §5.5, §30; thresholds §29  
**Design spec section:** §4 AI Architecture  
**Epic summary:** The category KB, Stage-1 auto-categorisation, Need/Want/Invest nature classification, and budgets with threshold alerts.

**Branch:** `feature/4-5-budget-alerts-80-100`

## Tracker

Progress and phase checklist: [4.5-budget-alerts-80-100-tracker.md](4.5-budget-alerts-80-100-tracker.md)

---

## Acceptance Criteria

- [x] Alerts fire at the configured thresholds (rows in `ai/rules/`, not hardcoded), deterministic on the injected `Clock`. — `RULE-BUD-ALERT` v1.0 holds 80/100; `BudgetRules` mirrors it and `RulebookDriftTest` was watched to fail on an `ai/`-only edit. No `now` is read in the engine; the repository stamps the clock.
- [x] The notification text passes the numeric guardrail before display (AI-ARC-004). — `NumericGuardrail.verify` runs in `AndroidBudgetAlertNotifier.notify` and a non-`Pass` result suppresses the post. Fail-closed; the narrowing vs `ai/chat/guardrail.md` is recorded in ADR-0019 and the class KDoc.
- [x] Covered by tests at the 79/80/100/101% boundaries. — `BudgetEngineTest`, thresholds read from `BudgetRules` rather than written as literals, plus 9 999 bps and `Int.MAX_VALUE` saturation.
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched. — no network on any path (P-04); `Long` paise and integer bps throughout (MNY-001/002); injected `Clock` (TIM-001); the band is shown with its inputs and its rule (P-02); no amounts or category names logged.
- [x] All Skill Rules and label-specific rules below are satisfied.
- [ ] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`). — **outstanding: the emulator run and `:core:database:connectedDebugAndroidTest` have not been performed** (see the tracker's Verification Log).

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `add-rulebook-rule` | Add/change any financial threshold as a versioned data row in ai/rules/ (never hardcoded). | `.claude/skills/add-rulebook-rule/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `workmanager-jobs` | Idempotent background workers on the injected Clock; offline-safe snapshots/alerts/widget refresh. | `~/.claude/skills/workmanager-jobs/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **P-07 Advice, never orders** - the app recommends and simulates; the user decides. No auto-executed money movement.

---

## Workflow Rules — Before Starting

- [ ] Dependencies (4.4) are complete or explicitly waived by the user
- [ ] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [ ] Read the SRS sections cited above (§5.5, §30; thresholds §29) and quote the requirement IDs back
- [ ] Read the design spec section: **§4 AI Architecture**
- [ ] Load the skill files listed above for this issue's labels
- [ ] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Thresholds are data rows (§29); guardrail every message (AI-ARC-004).

### Label-specific

1. Thresholds are versioned rule rows in `ai/rules/`, never hardcoded (§29).

### Epic-specific

1. Categorisation and budget thresholds are versioned data rows cited by ID - never hardcoded (§29).

---

## Workflow Rules — Definition of Done

- [ ] Acceptance criteria checked off above
- [ ] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [ ] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [ ] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [ ] No secrets/PII/amounts in logs or committed files
- [ ] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [ ] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [ ] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

Green on the JVM; **the device half is not done**.

```text
./gradlew ktlintCheck detekt lintDebug     # OK
./gradlew unitTests koverVerify            # OK
./gradlew :core:database:connectedDebugAndroidTest   # NOT RUN — needs a device
# /run + /verify on an emulator                       # NOT RUN
```

Two gates were **broken on purpose and watched to go red** before being trusted, each by editing
`ai/rules/rules-kb.json` and no Kotlin — which is what proves the `inputs.file` wiring is live and
the tests are not reporting `UP-TO-DATE` against a file they never opened:

| Edit | Test that went red |
|------|--------------------|
| `warn_pct` 80 → 75 | `RulebookDriftTest > the alert thresholds match RULE-BUD-ALERT` |
| `"warn_pct"` added to `RULE-BUD-PACE.params_json` | `RulebookDriftTest > the alert bands live on RULE-BUD-ALERT, not RULE-BUD-PACE` |

Both edits were reverted. A third defect was found by a test rather than a gate: with
`exceeded_pct` below 100 the EXCEEDED band is reached while the budget still has money in it, and
`spent - budgeted` went negative — the overspend is now floored at zero (ADR-0019).

Full timestamped log: [4.5-budget-alerts-80-100-tracker.md](4.5-budget-alerts-80-100-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `ai/rules/rules-kb.json` | New row `RULE-BUD-ALERT` v1.0 (`warn_pct` 80, `exceeded_pct` 100, `notify_once_per_band_per_month`); `_meta.version` 1.9.0 → 1.10.0; `RULE-BUD-PACE.source_note` repointed (prose only — no params change, version stays 1.0) |
| `ai/rules/rulebook.md` | `RULE-BUD-ALERT` added to the §29.2 table |
| `domain/engines/budget/.../BudgetRules.kt` | `warnPct` / `exceededPct` mirror + `ALERT` citation; `RULEBOOK_VERSION` → 1.10.0; `BPS_PER_PERCENT` |
| `domain/engines/budget/.../BudgetEngine.kt` | Third contract method `alert(...)`; `BudgetAlertInput`, `BudgetAlertBand`, `BudgetAlert` |
| `domain/engines/budget/.../BudgetAlertBands.kt` | **New** — the band decision (integer bps, no `Double`); overspend floored at zero |
| `domain/engines/budget/.../BudgetPace.kt` | **New** — pure move of the 4.4 pace maths out of the engine class (golden-file guarded) |
| `domain/engines/budget/.../DefaultBudgetEngine.kt` | `alert` implemented; pace helpers delegated |
| `domain/engines/budget/.../RulebookDriftTest.kt` | New threshold-drift case; `ALERT` added to the cited-rules loop; 4.4 tripwire **retargeted** to "the bands live on `RULE-BUD-ALERT`, not `RULE-BUD-PACE`" |
| `domain/engines/budget/.../BudgetGoldenTest.kt` + `test/resources/golden/budget.txt` | 8 `kind=alert` records; `MIN_RECORDS` 12 → 20; band/bps/overspend expectations and the meta-test extended |
| `domain/engines/budget/.../BudgetEngineTest.kt` | Boundary cases at 79/80/99.99/100/101%, zero budget, zero spend, `Int.MAX_VALUE` saturation, moved-bands, provenance |
| `domain/engines/budget/ENGINE.md` | `alert` contract, band formula table, `RULE-BUD-ALERT`, tests, version log |
| `core/model/.../NumericGuardrail.kt` | **New** — deterministic ₹/% verifier, fail-closed; KDoc names 9.7 as the full L3 gate (AI-ARC-004) |
| `core/model/.../NumericGuardrailTest.kt` | **New** — 10 cases, mostly refusals, incl. correct-but-unproduced arithmetic (GRD-003) |
| `core/database/.../entity/Entities.kt` | `BudgetAlertEntity` — `UNIQUE(profile_id, budget_id, month_start_iso_date, band)`, no amounts stored |
| `core/database/.../dao/Daos.kt` | `BudgetAlertDao` (`insertIfNew` = `IGNORE`, `observeForMonth`, `forMonth`); `DemoDao` wipe + row count |
| `core/database/.../CfoDatabase.kt` | `VERSION` 13 → 14; entity + DAO registered |
| `core/database/.../migration/Migrations.kt` | `MIGRATION_13_14` — `CREATE TABLE` + unique index + month index (non-destructive, DB-003) |
| `core/database/schemas/.../14.json` | **New** — exported schema, committed |
| `core/database/.../MigrationRoundTripTest.kt` | 13 → 14 case; the duplicate insert asserted as a `SQLiteConstraintException`, promotion still allowed |
| `core/database/.../MigrationSafetyTest.kt` | `budget_alert` exemption argued (no tombstone by design); guard test renamed and a new scoping test added |
| `data/repository/.../BudgetRepository.kt` | `CategoryBudgetAlert`; `observeAlerts` (derived from budgets, so it survives denied permission), `pendingAlerts`, `markNotified` (claim-by-insert) |
| `data/repository/.../BudgetRepositoryTest.kt` | Claim-once, band promotion, month rollover, demo isolation, stored rule citation |
| `app/src/main/AndroidManifest.xml` | `POST_NOTIFICATIONS` |
| `app/.../notification/CfoNotifications.kt` | **New** — `budget-alerts` channel |
| `app/.../notification/BudgetAlertNotifier.kt` | **New** — interface + Android impl; composes, guardrails, then posts; permission check inlined so lint can follow it |
| `app/.../di/NotificationModule.kt` | **New** — `@Binds` the notifier |
| `app/.../work/BudgetAlertWorker.kt` | **New** — daily worker; lock checked before the repository injects; claim → notify; no constraints (P-04) |
| `app/.../CfoApplication.kt` | Channel creation + worker scheduling |
| `app/src/main/res/values/strings.xml` | Channel name/description, four alert strings |
| `app/.../BudgetAlertWorkerTest.kt` | **New** — 8 cases incl. locked-app absence and claim-before-notify ordering |
| `feature/budgets/.../BudgetsUiState.kt` | `alerts`, `requestNotificationPermission`, derived band maps; `NotificationPermissionSettled` event |
| `feature/budgets/.../BudgetsViewModel.kt` | Alert stream collected; permission prompt raised after a write |
| `feature/budgets/.../BudgetAlertBanner.kt` | **New** — banner + band chip; words not only colour; error/tertiary roles, never money-in green |
| `feature/budgets/.../NotificationPermissionRequest.kt` | **New** — contextual API 33+ request; a no-op below it |
| `feature/budgets/.../BudgetsScreen.kt` | Banner rendered; per-row band chip |
| `feature/budgets/src/main/res/values/strings.xml` | Band labels, descriptions, two ICU plurals |
| `feature/budgets/build.gradle.kts` | `androidx.activity.compose` for the permission launcher |
| `feature/budgets/.../BudgetsViewModelTest.kt`, `.../BudgetsFlowTest.kt`, `.../FakeBudgetRepository.kt` | 12 new cases; fakes extended with alert fixtures |
| `docs/adr/0019-budget-alert-bands-mint-a-new-rule-row.md` | **New** — why a new row rather than a bump; the guardrail subset; what was watched to fail |
| `VERSION`, `CHANGELOG.md` | 0.4.3 → 0.4.4 |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/4.5-budget-alerts-80-100/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
