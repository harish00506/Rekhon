# [6.4] Allocation & diversification (AI-INV)

**Status:** Code complete and device-verified; not committed  
**Epic:** Epic 6 — Wealth: Loans & Investments  
**Priority:** Medium  
**Depends on:** 6.3  
**Assignee:** Harish G  
**Labels:** `wealth`, `investments`, `ai`

---

## Description

An allocation/diversification engine that classifies holdings into asset classes and flags concentration - advice with evidence, never an order.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §5.8; AI-INV  
**Design spec section:** §4 AI Architecture  
**Epic summary:** Credit cards, loans with amortisation, investment holdings with XIRR, allocation, and net-worth history - exact paise math throughout.

**Branch:** `feature/6-4-allocation-diversification-ai-inv`

## Tracker

Progress and phase checklist: [6.4-allocation-diversification-ai-inv-tracker.md](6.4-allocation-diversification-ai-inv-tracker.md)

---

## Acceptance Criteria

- [x] Allocation % by asset class is computed from holdings (numbers from math, P-03); concentration is flagged against rule rows.
- [x] Recommendations show inputs + rule (P-02) and are advisory (P-07).
- [x] Golden-file test.
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched.
- [x] All Skill Rules and label-specific rules below are satisfied.
- [x] Definition of Done met except steps 10's commit/push — git untouched pending the user's go-ahead (§7).

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `new-engine` | Scaffold a :domain:engines:* module (one interface, provenance, Hilt, ENGINE.md, tests). | `.claude/skills/new-engine/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `source-driven-development` | Ground framework/library use (Compose, Room, Hilt, ML Kit) in official docs before coding. | `~/.claude/skills/source-driven-development/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **AI-ARC-003/006** - every engine result carries provenance (engineId/version/inputWindow/computedAt/confidence/evidence).

---

## Workflow Rules — Before Starting

- [ ] Dependencies (6.3) are complete or explicitly waived by the user
- [ ] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [ ] Read the SRS sections cited above (§5.8; AI-INV) and quote the requirement IDs back
- [ ] Read the design spec section: **§4 AI Architecture**
- [ ] Load the skill files listed above for this issue's labels
- [ ] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Advice not orders (P-07); thresholds are data (§29).

### Label-specific

1. The LLM never computes a figure - it verbalises engine output, gated by the guardrail (P-03/AI-ARC-004).

### Epic-specific

1. Wealth math is exact: `Money` paise, rates in bps, 100% money-math coverage (MNY-001/002).

---

## Workflow Rules — Definition of Done

- [ ] Acceptance criteria checked off above
- [ ] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [ ] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [ ] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [ ] No secrets/PII/amounts in logs or committed files
- [ ] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [ ] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [ ] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

All green on 2026-08-29; full timestamped log in the tracker.

```text
./gradlew ktlintCheck detekt lintDebug     OK - no new warnings
./gradlew unitTests koverVerify            OK - engine >= 85%, money math 100%
# AVD CfoTest: split, all three flag kinds with their rule ids, coverage line,
# privacy blur, airplane mode, dark mode, 200% font - all observed
```

Six gates were broken on purpose and watched fail, then reverted (ADR-0005): the cap boundary, the
remainder distribution, the denominator, the unpriced exclusion, a rulebook-only edit, and a
deleted golden record.

Full timestamped log: [6.4-allocation-diversification-ai-inv-tracker.md](6.4-allocation-diversification-ai-inv-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `domain/engines/investment/src/main/kotlin/.../InvestmentRules.kt` | added - the typed rulebook mirror |
| `domain/engines/investment/src/main/kotlin/.../Allocation.kt` | added - the arithmetic |
| `domain/engines/investment/src/main/kotlin/.../InvestmentEngine.kt` | `allocation` + its result types |
| `domain/engines/investment/src/main/kotlin/.../DefaultInvestmentEngine.kt` | assembly, evidence, coverage; version 1.1 |
| `domain/engines/investment/build.gradle.kts` | `inputs.file(rules-kb.json)` |
| `domain/engines/investment/src/test/kotlin/.../{RulebookDrift,Allocation,AllocationProperty,AllocationGolden}Test.kt` | added |
| `domain/engines/investment/src/test/resources/golden/allocation.txt` | added - 9 independently-computed records |
| `domain/engines/investment/ENGINE.md` | allocation contract, rules, tests, version log |
| `data/repository/src/main/kotlin/.../InvestmentRepository.kt` | `observeAllocation()`; helpers to file scope |
| `data/repository/src/test/kotlin/.../InvestmentRepositoryTest.kt` | six allocation cases |
| `feature/accounts/src/main/kotlin/.../Allocation{UiState,ViewModel,Screen}.kt` | added |
| `feature/accounts/src/main/kotlin/.../{AccountsActions,AccountsScreen}.kt` | `onOpenAllocation` + entry button |
| `feature/accounts/src/main/res/values/strings.xml` | 20 `allocation_*` strings |
| `feature/accounts/src/test/kotlin/.../Allocation{ViewModel,Screen}Test.kt` | added |
| `feature/accounts/build.gradle.kts` | release-variant exclusion for the Compose test |
| `app/src/main/kotlin/.../navigation/{CfoRoute,CfoNavHost}.kt` | `data object Allocation` + destination |
| `ai/orchestrator/engine-registry.yaml` | `AI-INV` 1.0 -> 1.1 |
| `docs/adr/0029-the-portfolio-is-the-investable-accounts.md` | added |
| `DECISIONS.md`, `FLOW.md`, `CHANGELOG.md`, `VERSION`, `app/build.gradle.kts` | index row, §2.4, 0.6.4, versionCode 25 |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/6.4-allocation-diversification-ai-inv/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
