# [3.9] SMS parsing (opt-in, on-device)

**Status:** In review (implementation complete, emulator gate outstanding)  
**Epic:** Epic 3 — Transactions & Capture  
**Priority:** Medium  
**Depends on:** 3.1  
**Assignee:** Harish G  
**Labels:** `sms`, `security`

---

## Description

Opt-in, on-device SMS transaction parsing (bank alerts → draft transactions) behind an explicit, revocable consent - no message content ever leaves the device.

Master blueprint (source of truth): `docs/init/AI_Personal_CFO_SRS_v1.7.pdf` - cite the requirement
IDs in code and commits. Planning design spec: [`2026-07-17-ai-personal-cfo-design.md`](../superpowers/specs/2026-07-17-ai-personal-cfo-design.md).
Master workflow: [`00-issue-workflow.md`](00-issue-workflow.md). Binding rules: `CLAUDE.md`.

**SRS sections:** §18, §23; P-01  
**Design spec section:** §8 Security & Privacy  
**Epic summary:** ≤3-tap transaction entry plus automated capture - transfers, splits, recurring detection, OCR receipts, and opt-in SMS parsing - all provenance-tagged.

**Branch:** `feature/3-9-sms-parsing-opt-in-on-device`

## Tracker

Progress and phase checklist: [3.9-sms-parsing-opt-in-on-device-tracker.md](3.9-sms-parsing-opt-in-on-device-tracker.md)

---

## Acceptance Criteria

- [x] SMS read is opt-in via explicit consent (default off, revocable, P-01); disabled means zero SMS access.
      Two gates, consent first (ADR-0013). `SmsRepository` is the single chokepoint; `SmsRepositoryTest`
      proves it with a reader that **throws** if called, because an empty result is also what a reader
      that *was* called and found nothing returns.
- [x] Parsing runs on-device to draft txns for review (source = SMS); no raw SMS stored beyond what is needed.
      `sms_draft` has **no `body` column** — the criterion made structural, asserted by `MigrationSafetyTest`.
- [x] The parser is deterministic and tested on a fixture of anonymised templates.
      50 frozen messages in `eval/sms.txt`: 30 real movements, 20 that merely mention money.
      Gates: amount + direction >= 95%, **100% rejection** of the twenty. All currently 100%.
- [x] Golden rules (P-01...P-08) respected; money/time rules hold wherever amounts or dates are touched.
      No `Double` in either new module; every amount through `MoneyFormatter.parse`; the parser reads
      no clock, and the booking day is converted in the profile zone by the repository (TIM-001/002).
- [x] All Skill Rules and label-specific rules below are satisfied.
- [ ] Definition of Done met (`00-issue-workflow.md` steps 9-11; `/pre-merge`) — emulator run outstanding.

---

## Skill Rules (load before coding)

Load and follow these skills **before** implementing:

| Skill | When to use | Path |
|-------|-------------|------|
| `security-review` | Mandatory for crypto, keys, SMS, backup, or consent changes (§23, SEC-*). | `~/.claude/skills/security-review/SKILL.md` |
| `source-driven-development` | Ground framework/library use (Compose, Room, Hilt, ML Kit) in official docs before coding. | `~/.claude/skills/source-driven-development/SKILL.md` |
| `test-driven-development` | Write the failing test from the acceptance criteria first, then code to green. | `~/.claude/skills/test-driven-development/SKILL.md` |
| `incremental-implementation` | Land in small reviewable slices; feature-flag incomplete work. | `~/.claude/skills/incremental-implementation/SKILL.md` |
| `debugging-and-error-recovery` | Bugs: observe → reproduce → hypothesize → isolate → fix → verify. | `~/.claude/skills/debugging-and-error-recovery/SKILL.md` |

Commands: **`/ponytail`** (laziest correct diff) - **`/run`** + **`/verify`** (drive the app) -
**`/pre-merge`** (Definition-of-Done gate). Read each skill file before coding the matching area.

---

## Guiding Principles (CLAUDE.md §1 / design spec §2)

- **P-03 Numbers from math, words from AI** - engines compute every figure; the LLM only verbalises.
- **P-01 Privacy first** - no financial data leaves the device without explicit, revocable, per-feature consent.
- **P-02 Show the work** - every output shows its inputs + the rule/model that fired + a plain-language reason.
- **P-08 Deterministic & testable** - fixed input → fixed output; randomness only via an injected, seedable source.
- **SEC-003 / P-01** - Tink/Keystore only (no hand-rolled crypto); every off-device path is consent-gated.

---

## Workflow Rules — Before Starting

- [ ] Dependencies (3.1) are complete or explicitly waived by the user
- [ ] Read `docs/issues/00-issue-workflow.md` and `CLAUDE.md`
- [ ] Read the SRS sections cited above (§18, §23; P-01) and quote the requirement IDs back
- [ ] Read the design spec section: **§8 Security & Privacy**
- [ ] Load the skill files listed above for this issue's labels
- [ ] List assumptions and challenge each one before coding

## Workflow Rules — While Solving

### Universal (apply to every issue)

1. Define expected vs actual behaviour with evidence (logs, tests).
2. Reproduce before fixing; binary-search to isolate the root cause.
3. Form testable hypotheses; switch to formal debugging after 10 min of guessing.
4. Smallest correct diff; match the surrounding code style.
5. Verify the fix with the original reproduction case.
6. **Respect the boundaries** - `feature → domain → data/core` (ARC-001); engines pure-Kotlin (ARC-002); repositories are the only DAO touchers (ARC-005).

### Issue-specific (from the SRS)

1. Explicit, revocable consent; content stays on-device (P-01).

### Label-specific

1. No hand-rolled crypto - Tink/Keystore only (SEC-003); no PII/amounts in release logs.

### Epic-specific

1. Add-txn stays ≤ 3 taps; every captured record is provenance-tagged by source (FR-TXN-002/FR-TXN-009/AI-ARC-003).

---

## Workflow Rules — Definition of Done

- [ ] Acceptance criteria checked off above
- [ ] Every new/changed function has a doc comment (why/what/result/changelog/inputs/outputs) and a test
- [ ] Tests pass for every touched suite; coverage gate met (engine ≥ 85%, money 100%)
- [ ] Works offline - verified with an airplane-mode case where a data path is touched (P-04)
- [ ] No secrets/PII/amounts in logs or committed files
- [ ] No new lint/detekt warnings; strings externalised; dark mode + accessibility verified (if UI)
- [ ] App run and observed working on an emulator/device (`/run` + `/verify`) - a green build does not close the issue
- [ ] Tracker updated with the Verification Log; `VERSION` + `CHANGELOG.md` bumped at ship

---

## Verification

_Not started._ On completion, record the commands + results here and in the tracker's Verification Log:

```text
./gradlew ktlintCheck detekt lintDebug
./gradlew unitTests koverVerify
# /run + /verify on an emulator; connectedDebugAndroidTest for core flows (incl. airplane-mode)
```

Full timestamped log: [3.9-sms-parsing-opt-in-on-device-tracker.md](3.9-sms-parsing-opt-in-on-device-tracker.md#verification-log).

---

## Files Changed

| Path | Action |
|------|--------|
| `ai/rules/rules-kb.json`, `ai/rules/rulebook.md` | `RULE-SMS-PARSE` added; `_meta` -> 1.8.0 |
| `core/model/.../SmsMessage.kt` (+ test) | The boundary type both sides name (ARC-002) |
| `domain/engines/sms/**` | **New module** — parser, `SmsRules`, `ENGINE.md`, 41 tests, `eval/sms.txt` |
| `data/sms/**` | **New module** — `SmsInboxReader` + the only `READ_SMS` declaration, 12 tests |
| `core/database/.../Entities.kt`, `Daos.kt`, `CfoDatabase.kt`, `migration/Migrations.kt`, `schemas/12.json` | `sms_draft` at v12; `findAlertDuplicateCandidates` |
| `core/database/src/test/.../MigrationSafetyTest.kt` | The `sms_draft` exemption + its two replacement assertions |
| `core/database/src/androidTest/.../MigrationRoundTripTest.kt` | 11 -> 12 round trip, incl. the unique index and revocation |
| `core/datastore/.../cfo_settings.proto`, `Model.kt`, `SettingsStore.kt` | `sms_scan_cursor_id` (field 17) |
| `data/repository/.../SmsRepository.kt` (+ test), `RepositoryFactory.kt` | The gate, the cursor, the decisions |
| `feature/transactions/.../SmsDrafts{Screen,ViewModel,UiState}.kt` (+ 2 test files) | The review screen |
| `feature/transactions/src/main/res/values/strings.xml` | The screen's copy, externalised |
| `app/.../work/SmsScanWorker.kt`, `CfoApplication.kt` | The daily scan |
| `app/.../di/EngineModule.kt`, `RepositoryModule.kt`, `navigation/CfoRoute.kt`, `CfoNavHost.kt` | Wiring + the typed route |
| `domain/engines/{quicksetup,recurring,receipt,sms}/build.gradle.kts` | **Fix:** the rulebook is now a declared test input |
| `docs/adr/0013-read-sms-play-policy-and-the-gated-inbox.md` | **New ADR** |
| `CHANGELOG.md`, `VERSION` | 0.3.10 -> 0.3.11 |

---

## Sub-tasks

No sub-tasks yet. Break into `docs/features/3.9-sms-parsing-opt-in-on-device/tasks/` when this issue is too
large for one PR (use `docs/features/_TASK_TEMPLATE.md`).
